# Component: Model Registry

> STATUS: IN PROGRESS — registry structure, selection mechanism, escalate-on-retry, storage location, pricing table, and fallback-past-top-tier all resolved **and implemented**: `activities/activities/model_registry.py`, wired into `model_call.py`/`llm.py`/`turn.go`, and used by `context-slot.md`'s compression threshold (no longer blocked). Verified live against the real cluster and the real configured model — see Notes Log. Multi-provider abstraction is the one remaining open item.

### Role (one line)
Lets the harness select an appropriate model per task at runtime — which model to call, based on what the turn/subagent/step actually needs — replacing today's single model hardcoded into worker startup with no selection mechanism at all.

### Why this exists (recap)
`activities/activities/tenant_worker.py` constructs exactly **one** `AsyncOpenAI` client at process startup, from `PIONEER_API_KEY`/`PIONEER_BASE_URL`/`PIONEER_MODEL`/`PIONEER_MAX_TOKENS` env vars, and injects it once into `ModelCallActivity.__init__`. Every `ModelCall`, for every turn and every subagent at every recursion depth, uses that same one model. There is no mechanism today for, e.g., a subagent doing a narrow, cheap task to use a smaller/faster model while the top-level turn uses a stronger one, or for a tenant to configure more than a single model choice.

### Responsibilities (from architecture)
- Define what models are available: name, provider/base URL, context window size, cost (`$`/input token, `$`/output token), and capability tier.
- Decide selection: how `ModelCall` picks which model to use for a given turn/subagent/step — static per-tenant default vs. dynamic per-task choice.
- Expose per-model context-window size so the context slot (`components/context-slot.md`) can make its compression threshold model-aware, instead of the single hardcoded `budgetTokens` constant in `turn.go` today.
- Stay inside the reference-passing / tenant-isolation contract already established (`components/temporal-workflow.md`, `components/multi-tenancy.md`): model credentials remain tenant-worker-side only, never cross into the shared loop-worker.

### Resolved: Registry Structure — Modality × Tier
```
registry[modality][tier] -> model config
```
- `modality: language` — real, three tiers: `fast | medium | expert`.
- `modality: vision | audio | video` — **placeholders only for now**, no real dispatch logic. Nothing in the current toolset (`shell_exec` plus fixture stubs) calls any of these; the registry's shape accommodates them cheaply, but selection logic for them is explicitly deferred until a real consumer exists.

### Resolved: Selection Mechanism — Model Self-Declares the Next Step's Hint
`ModelCallOutput` gains a `next_step_hint: {modality, tier}` field — the model, as part of producing its current response, also declares what the *next* step needs. This rides along on output the model already has to produce, so it costs no extra call, unlike a separate classification hop. The hint threads into the following `ModelCallInput`; the **activity** (Python, tenant-worker) resolves it against the registry and picks the concrete model — the Go workflow passes the hint through as opaque data and never needs to interpret it, consistent with the tenant-isolation contract.

- **Bootstrapping**: the first `ModelCall` of a turn has no prior hint. Defaults to `{language, medium}`.
- **Applied every step, not damped.** The model re-declares a hint at every reasoning step, since step complexity genuinely varies step to step — a deliberate choice, not an oversight of the cache-prefix-stability concern named in `components/context-slot.md`. The actual cache cost is proportional to how often the declared tier *changes* between consecutive steps, not to how often it's declared — a run of several same-tier steps in a row stays cache-stable even though each one re-declares.

### Resolved: Escalate-on-Retry
When a `ModelCall` attempt fails (e.g. unparseable tool-call output from a fast-tier model), Temporal's own retry mechanism fires. The activity reads its own attempt number via `activity.GetInfo(ctx).Attempt` and escalates the tier by one step per attempt (`fast → medium → expert`), capped at `expert` — never escalates past the top tier. `RetryPolicy.MaximumAttempts` should be sized to match the number of language tiers (3) so the retry ladder and the escalation ladder line up, rather than being an arbitrary retry count picked independently.

### Resolved: Storage — Env Vars, Not Postgres
Deployment-time configuration, same category as today's `PIONEER_*` — not dynamic runtime data, so a Postgres table would be solving a problem this doesn't have. `LANGUAGE_<TIER>_MODEL`/`LANGUAGE_<TIER>_CONTEXT_WINDOW` per tier, wired through `deploy/helm/agent-harness-tenant`'s `llm.tiers.{fast,medium,expert}.{model,contextWindow}` (only rendered when actually set). Falls back to `PIONEER_MODEL` for any unconfigured tier, so a deployment that's only ever set the single existing value keeps behaving exactly as before — every tier resolves to the same model.

### Resolved: Pricing Table
`ModelConfig` gains three rates, not one: `input_cost_per_token`, `output_cost_per_token`, `cached_input_cost_per_token`. Providers price these separately (input/output routinely 3-5x apart; a prompt-cache hit is priced separately again, further discounted from regular input) — a single `cost_per_token` field would already be wrong for any real provider's pricing sheet. Configured per-tier via `LANGUAGE_<TIER>_{INPUT,OUTPUT,CACHED_INPUT}_COST_PER_MILLION_TOKENS` (per-million, matching how providers publish pricing — a raw per-token float would mean typing `0.0000027` into a Helm value). Unset defaults to `0.0`, meaning "no cost data," not "free" — same honest-scope choice `budget-guardrails.md` already made for token-count metrics shipping ahead of cost metrics. This closes model-registry's side of that component's stated dependency; budget-guardrails.md still needs to actually consume this table into real cost metrics, not built here.

### Resolved: Fallback Beyond Escalate-on-Retry
When `ModelCall` still fails after exhausting `RetryPolicy.MaximumAttempts: 3` (i.e. even the `expert` tier failed), `turn.go` now routes every early-return failure in `TurnWorkflow` — this one included — through a shared `failTurn` helper: marks the turn `failed` in Postgres (`Persist`, a value the `turns.status` CHECK constraint already allowed but nothing wrote), inserts a synthetic assistant-role error message so `Deliver` has something real to send, and delivers it (top-level turns only). Before this fix there was no defer/recover anywhere in `TurnWorkflow`, so a failure here — or from either of `TurnWorkflow`'s other two early-return sites, which shared the identical defect — skipped Persist/Deliver entirely: the turn's Postgres row stayed stuck at `running` forever and the user never received any response or error at all. Not a designed fallback that happened to be minimal; an actual silent drop, confirmed by reading the code path, not assumed.

### Open Questions / To Design
- Multi-provider support: is this OpenAI-compatible-only, matching today's `llm.py` assumption, or does it need to abstract across genuinely different provider APIs (different request/response shapes, not just different base URLs)? Deferred — no second provider exists yet to design against concretely.
### Notes Log
- 2026-08-23 (later): **Resolved and implemented two more open items**: the pricing table (`ModelConfig.{input,output,cached_input}_cost_per_token`, env-var-configured via `LANGUAGE_<TIER>_*_COST_PER_MILLION_TOKENS`, Helm-wired through `llm.tiers.{fast,medium,expert}.costPerMillionTokens.{input,output,cachedInput}` — verified via `helm lint`/`helm template`, unset tiers correctly emit no env vars, a configured tier renders the expected three) and fallback-beyond-escalate-on-retry (`turn.go`'s new `failTurn` helper, applied to all three of `TurnWorkflow`'s early-return sites — not just the `ModelCall` one, since the other two, both `InsertMessage` failures, shared the identical silent-drop defect: no `defer`/`recover` anywhere in the function meant any of them skipped Persist/Deliver and left the turn's Postgres row stuck at `running` with no response ever delivered to the user; confirmed by reading the code path directly, not assumed). `go build`/`go vet` clean.

  Verified live, not just code-reviewed: scaled the cluster's tenant-worker/loop-worker Deployments to 0, ran locally-built binaries against the real port-forwarded Temporal/Postgres, pointed `PIONEER_BASE_URL` at an unreachable local port (`http://127.0.0.1:1`) to force real, fast, zero-cost connection failures, and signalled a fresh session. Confirmed via `tenant-worker.log` that `ModelCall` genuinely retried 3 times (escalate-on-retry exercised live for the first time, not just code-reviewed) before `failTurn` fired. Confirmed via direct Postgres query: `turns.status='failed'` with a real `completed_at`, and a real synthetic `assistant`-role message ("Something went wrong processing this turn.") in `messages` at seq 1, right after the original user message at seq 0 — exactly what `Deliver`'s log line also showed it sent. Cluster Deployments restored afterward.
- 2026-08-23: **Implemented**, not just designed — `activities/activities/model_registry.py` (`resolve`, `escalate`, `default_hint`). `declare_next_step_hint` added as a real tool in `llm.py`'s `TOOLS_SCHEMA` (the model includes it alongside its other tool_calls in the same response, per "Resolved: Selection Mechanism" — needed an explicit system-prompt instruction to activate, unlike the reverted search_tools-before-shell_exec rule, since the model has no structural way to know this protocol exists without being told). `call_model` extracts and strips it from the real `raw_tool_calls` before they reach `tool_call.py`/`turn.go`. `model_call.py` reads `activity.info().attempt` and escalates the tier by one step per retry attempt before resolving. `turn.go` threads `ModelCallOutput`'s hint fields into the next `ModelCallInput` opaquely, and sets `RetryPolicy.MaximumAttempts: 3` on `ModelCall`'s own `ActivityOptions` to match the tier ladder. Also closed `context-slot.md`'s "blocked on model-registry.md" compression-threshold item as a natural side effect: `ModelCallOutput.ContextWindow` now lets `turn.go` size the soft/hard threshold as a fraction of the real active model's context window, falling back to the old static constants only on the fixture path (`ContextWindow == 0`).

  Verified live against the real cluster and the actual configured model (DeepSeek-V4-Pro via Crusoe), not just code-reviewed: decoded the real `ModelCallOutput` payload from Temporal's workflow history — the model correctly complied with `declare_next_step_hint` on its first real call, and made a genuinely sensible judgment call (`tier=fast` after deciding the next step was just "report a shell command's output," matching that tier's described semantics) rather than just mechanically satisfying the instruction. Confirmed the hint threading is real, not just plausible: call 1's output declared `next_hint_tier=fast`, call 2's own scheduled input showed `hint_tier=fast` exactly, pulled directly from Temporal history, not inferred. Escalate-on-retry was not exercised live (would need a real ModelCall failure to trigger) — code-reviewed only.
- 2026-08-16: Introduced as a scaffold, one of four (later five, with budget/guardrails split out separately) components identified while discussing how to make the harness robust for genuinely complex, multi-step tasks. Grounded in `tenant_worker.py`'s current single-client-at-startup construction, audited the same day — not yet designed.
- 2026-08-17: Resolved the registry structure (modality × tier, language real with three tiers, vision/audio/video placeholder-only), the selection mechanism (model self-declares the next step's hint at zero extra call cost, resolved activity-side, applied every step by deliberate choice rather than damped), and escalate-on-retry via Temporal's own attempt-count mechanism. Storage, multi-provider abstraction, and fallback-past-top-tier remain open.
- 2026-08-17 (later): Resolved `components/memory-slot.md`'s extraction-model dependency — extraction reuses the `fast` language tier directly, no separate named purpose/hint needed.
