# Component: Gateway
## (Per-Platform Ingestion & Delivery)

> STATUS: IN PROGRESS — **now an index doc, 2026-08-23** (see Notes Log). Core flow, deployment model, and reliability model resolved here as the shared contract every gateway kind implements. **Deployment topology revised the same day (later)**: per-tenant, not shared-per-platform — see "Resolved: Per-Tenant Deployment" below, which supersedes the original "one gateway type per platform, shared across tenants" framing (a real, previously-unreconciled conflict with `multi-tenancy.md`'s credential-isolation principle, caught directly rather than carried forward silently). Platform-specific client-facing design lives in `gateway/<kind>.md` — currently just `gateway/web.md`, the first real platform being built. Everything below that's illustrated with Discord (connection leasing, failure analysis) is general reasoning about that platform *category*, not a real, currently-being-built target.

### Role (one line)
The platform-integration boundary: normalizes whatever a messaging platform sends into a generic `MessageEvent`, submits it durably into Temporal, and delivers the eventual response back to the client. Deliberately platform-generic engineering at this level — nothing in *this* doc is agent-specific or platform-specific; it's the same shared contract any chat-connected system's gateway layer would need, regardless of which platforms sit on top of it.

### Gateway Kinds
| Kind | Doc | Status |
|---|---|---|
| Web (first-party browser chat, `agent-web`) | `gateway/web.md` | IN PROGRESS — first real platform, implemented and live-verified |
| Discord | `gateway/discord.md` | IN PROGRESS — connected live; ingestion, mention/reply/personal-DM trigger detection, and outbound delivery all built (not yet redeployed for a full live round-trip confirmation) |
| Discord Voice | `gateway/discord-voice.md` | IN PROGRESS — core cascaded pipeline built (capture/VAD/STT/turn/TTS/playback), not yet live-verified; see "Resolved: Voice Platforms — Cascaded Architecture" below for the reference shape every voice platform follows |
| Webhook-style (WhatsApp/Telegram) | *(none yet)* | Illustrative only, in this doc's category framing below — not a real build target |

### Superseded: One Gateway Type Per Platform, Shared Across Tenants
**2026-08-23.** Originally framed as "one gateway per source" meaning one process per channel/chat, then revised to "the deployment unit is one gateway type per platform (`DiscordGateway`, `WhatsAppGateway`, ...), each independently horizontally scaled — internally handling many users/channels/*tenants* at once." That framing is wrong, caught directly rather than carried forward: it means one shared process holding real per-tenant secrets (Postgres passwords, platform API tokens, Clerk keys) for *every* tenant it serves — a direct conflict with `multi-tenancy.md`'s own core principle, *"no process, database, or worker ever holds more than one tenant's credentials or raw content,"* never reconciled against this doc because this doc predates that principle being fully worked out. See "Resolved: Per-Tenant Deployment" below for the corrected shape.

The distinction this section *did* get right, and which still matters under the corrected shape: the right replication/addressing strategy differs fundamentally by platform category —
- **Connection-based platforms** (Discord, Slack Socket Mode, Signal) hold a live, stateful socket. Something has to arbitrate which process holds it — originally "the platform's own shard partitioning," now "a Postgres-backed connection lease," see below.
- **Webhook-based platforms** (WhatsApp Business API, Telegram, Slack Events API, and first-party polling clients like `gateway/web.md`) have no persistent connection — inbound arrives as a stateless HTTP POST, outbound is a stateless HTTP call (or, for a polling client, no outbound call at all — see `gateway/web.md`'s "delivery collapses" finding). No partitioning concept exists; replicas are fully interchangeable.

### Resolved: Per-Tenant Deployment — One Gateway Process Per Tenant, Every Platform Kind via Goroutines
Not shared across tenants (see Superseded section above), and not one process per (tenant × platform) either — **one Gateway process per tenant**, written in Go, deployed as part of the `agent-harness-tenant` chart (alongside `tenant-worker` and that tenant's own Postgres — *not* part of `agent-harness-shared`), holding only that one tenant's own credentials. Internally, it runs one goroutine (or HTTP handler) per platform kind that tenant has actually configured — a tenant using both Web and Discord runs both inside the *same* process, not two separate deployments.

This isn't a new pattern — it directly mirrors something already built and running in this codebase: `loop-worker` already runs one `Client`+`Worker` pair per tenant namespace it serves, concurrently, in the same process, via static config at deploy time (`multi-tenancy.md`'s "Mechanics — resolved 2026-08-14, static config"). Same shape here, just "per configured platform kind" instead of "per tenant namespace," and per-tenant instead of shared.

**Why Go, and why this resolves cleanly**: the language choice (Go, matching `loop-worker`, at the user's direction) initially seemed to conflict with per-tenant credential isolation — Go's *shared* process (`loop-worker`) is deliberately kept credential-free under the reference-passing contract. But that conflict only existed because "Go" and "shared across tenants" were being conflated. A Gateway written in Go that's *also* deployed per-tenant (its own binary, its own deployment, this tenant's own credentials only) has exactly the same isolation shape `tenant-worker` (Python, per-tenant) already has — the language was never the actual constraint.

### Resolved: Package Structure — One Package Per Platform + a Shared Core (2026-08-30)

The gateway binary was one flat `package main` (~34 files, ~6,200 lines) with every concern — Web HTTP, Discord ingestion, Discord voice capture, leasing — hung off a single `*server` god struct. Refactored into `workflows/internal/gateway/`:

| Package | Owns |
|---|---|
| `core` | `MessageEvent`, `Ingest` (session resolve → dedup → `SignalWithStart`), `SessionKeyFor` identity, per-platform system prompts. Imports no platform adapter, no `net/http`, no `discordgo` — the dependency rule made concrete. |
| `lease` | The Postgres connection lease (Discord text + voice; Web needs none). |
| `speech` | OpenAI-compatible STT/TTS clients + transcript-text helpers (`SanitizeForSpeech`, `IsFillerOnly`, `NormalizeForSpeech`). |
| `web` | `POST /send`, `GET /poll`, `POST /respond`, `GET /sessions` + Clerk auth + the Web `session_id` vocabulary. |
| `discord` | Discord text: the leased `*discordgo.Session`, ingestion, `DiscordDeliver*` activities, slash commands, `/mode`, user-input buttons, voice-message transcription. |
| `discordvoice` | Discord voice: capture loop, `VoiceDeliver*` activities, the whole voice DSP stack (VAD, EOT, Opus, barge-in, filler, latency). |
| `discordui` | Discord message components (the user-input button block) shared by `discord` and `discordvoice`. |

**Dependency DAG (acyclic):** `main → {web, discord}`; `web → core`; `discord → {discordvoice, speech, lease, core, discordui}`; `discordvoice → {speech, lease, core, discordui}`; `core, lease, speech → infra only`. `discord` owns the Discord connection and constructs `discordvoice.Voice` as a child (a voice connection is negotiated as a child of the open text gateway session), delegating `/join`, `/leave`, and voice interactions to it.

**Deliberately minimal.** No `Platform` interface — Web is "register routes on a mux," Discord is "run a leased websocket goroutine"; those are different enough, and there are few enough, that `main` wiring each explicitly beats a thin polymorphic abstraction. The `Deliver*` activities stay platform-specific (`VoiceDeliverChunk` returns `(bool, error)` for barge-in, `DiscordDeliverChunk` returns `error` — a real, documented difference), so no forced `Deliverer` interface. The `*server` god struct is replaced by per-package constructor dependencies (`core.NewIngestor`, `lease.NewManager`, `web.New`, `discord.New`), and `main.go` is a ~60-line composition root. Behavior unchanged; `go build`/`vet`/`gofmt`/`test` all green, `git` records every move as a rename. Slash-command registration and shutdown teardown moved to one place each (`discord.registerCommands`, `discordvoice.Voice.TeardownAll`).

### Resolved: Always Async — No Sync-Required Platforms Supported
Even platforms that *offer* a synchronous response path (e.g. Slack slash commands, technically answerable within the same request) are deliberately treated as async here: ack fast, deliver the real answer later via whatever async mechanism the platform provides (a callback URL, a separate outbound API, the platform's own live connection). Holding an HTTP request or blocking a thread open for the duration of a full agentic turn (possibly involving deep tool-call sequences or subagent recursion) doesn't scale and doesn't fit this design's turn-duration profile.

**Explicit scope boundary:** a platform with *no* async mechanism at all — the full final answer must be returned on the same inbound request, no callback URL, no separate send API — is simply **not supported** by this gateway design. This was considered (a request-scoped Postgres `LISTEN`/`NOTIFY` wait with a bounded timeout was designed as a possible mechanism) and explicitly rejected in favor of keeping the gateway uniformly async. Stated plainly so it isn't rediscovered as a surprise later.

### Resolved: Inbound Flow
1. Raw event arrives at a gateway replica — via its own websocket (connection-based, only reaches the replica currently holding that platform's connection lease) or via HTTP POST (webhook-based, any replica, load-balanced).
2. Normalize into a `MessageEvent` — the generic/agentic boundary. Nothing past this step is platform-specific.
3. Resolve `session_key`, run the auth check.
4. **Dedup check-then-insert against `ingested_messages(platform, platform_message_id)`** — a ledger symmetric to `delivered_responses` on the outbound side. Needed because webhook platforms redeliver at-least-once; without this, a redelivered webhook would double-signal. If already present, skip to step 6 (ack only) — the message was already durably submitted.
5. `SignalWithStart` the session coordinator (workflow ID = `session_key`), passing the message content as the signal payload. This is the durable submission — Temporal records the payload in workflow history, so **the gateway does not separately write the message body to Postgres.** The full-content persist into `messages` happens later, as an activity inside the coordinator/turn flow, sourced from the signal payload — not duplicated at the gateway. (This removes a real redundancy an earlier version of this design had: writing the same payload durably twice, once to Postgres directly and once via the signal.)
6. Ack the platform (webhook: return 200 now; connection-based: no separate ack, just don't block).
7. Gateway's job for this message ends here — no correlation state kept, no waiting on the turn to finish. This is what "always async" buys structurally: nothing pending, nothing to garbage-collect if the gateway restarts.

*(Slash commands like `/approve`/`/stop` still route differently — unresolved, unchanged by this design.)*

### Resolved: Generic `MessageEvent` Shape
Grounded in what `gateway/web.md`'s real implementation actually needs, extended once a second real platform (`gateway/discord.md`) needed two more fields — not speculated ahead of either:

```go
type MessageEvent struct {
    Platform          string // "web" today, "discord" designed next
    ChannelID         string // scopes the CHANNEL — shared across every user posting in it
    User              string // who sent THIS message, within ChannelID — orthogonal to channel scoping
    Content           string
    PlatformMessageID string // dedup key — step 4's ingested_messages(platform, platform_message_id)
    Discriminator     string // which of possibly-many sessions in this channel — see "Resolved: Multi-Session Channels" below
    ParentSessionKey  string // set only when Discriminator resolves to a NEW session — which session it branched from
}
```

`ChannelID` and `User` are deliberately separate fields, not one: a session is scoped to the *conversation*, not to whichever user happens to send a given message — see "Resolved: Multi-Session Channels" below for what "the conversation" means once a channel can hold more than one. Collapsing the two would merge different users' DMs into one session on a platform where channel and user diverge. Web happens to have `ChannelID == User` (no group-chat concept there), which is exactly why this distinction was easy to miss until named directly — Web's own instance can't surface the bug this would cause elsewhere.

`session_key` itself is *not* a `MessageEvent` field — it's derived downstream (`sessionKeyFor(event.Platform, event.ChannelID, event.Discriminator)`), not resolved per-platform and threaded through as a redundant, driftable value.

**Known gap, not fixed by this shape:** `User` isn't persisted anywhere past `MessageEvent` for Web today — `messages` has no `author`/`platform_user_id` column, only `role`. Real for group-chat attribution, and `gateway/discord.md`'s own ambient-message buffer *does* capture it (needed for reply-chain resolution regardless) — but nothing folds it into the `messages` table itself yet. Left as a follow-on for whichever platform first needs the transcript itself to carry authorship, not built ahead of that need.

Implementation: `workflows/cmd/gateway/inbound.go` (`MessageEvent`, `submitMessageEvent` — steps 3-6 above, platform-agnostic, real for both platforms now). Web's own normalize (`send.go`) always sends `Discriminator: "channel:" + ChannelID`, `ParentSessionKey: ""`, matching its existing single-session-per-user behavior exactly (live-verified: format unchanged). Discord's own normalize (`discord.go`) computes both for real — root-collapse discriminator resolution against `discord_ambient_messages` — live-verified against real Postgres/Temporal, though not yet against a real Discord connection (`gateway/discord.md`'s own Notes Log has the detail).

### Resolved: Multi-Session Channels — Sessions as a Graph, Not a List
A channel (Web's own private per-user "channel," a Discord guild text channel, a Slack channel) can hold more than one session. `gateway/discord.md`'s reply-threading requirement is the concrete driver: a reply to some earlier message starts a conversation that should have its own scoped context, distinct from the channel's general ongoing conversation — a session *graph* per channel, not one flat session.

**`Discriminator` — which session, within a channel, does this message belong to.** Always populated, `<type>:<id>` format, never a magic empty-string special case:
- The channel's own main/default session: `channel:{channelID}` — stable and deterministic, since `channelID` is always known directly from the message, no lookup needed.
- A reply-scoped sub-session: `<type>:<id>` sourced from whatever platform-native reply anchor exists — e.g. Discord's `reply_to_platform_message_id:{rootMessageID}`, a hypothetical Slack integration's `thread_ts:{ts}`. The exact resolution *policy* (which id, resolved how) is platform-specific and lives in each platform's own `gateway/<kind>.md`, not here.

`session_key` itself still gets built per-platform (`sessionKeyFor(platform, channelID, discriminator)` in `workflows/cmd/gateway/auth.go`), and for Web specifically the *string format* stays exactly what it is today (`agent:main:web:user:{id}`, no discriminator suffix) even though `Discriminator` is now always conceptually `channel:{id}` under the hood — purely to avoid changing the format of an already-running, already-verified real session. A brand-new platform (Discord) has no such constraint and can embed the discriminator directly, e.g. `agent:main:discord:channel:{channelID}` vs. `agent:main:discord:channel:{channelID}:thread:{rootID}`.

**`ParentSessionKey` — the graph structure, and why it's Postgres data, not a Temporal parent/child relationship.** When `Discriminator` resolves to a session that doesn't exist yet, `ParentSessionKey` names which session it branched from; `sessions` gets a new nullable `parent_session_key` column recording it. Considered and rejected: making a reply-session a genuine Temporal *child workflow* of the channel's main session's `CoordinatorWorkflow`. Rejected because `CoordinatorWorkflow` routinely self-terminates on idle timeout and gets recreated fresh via `SignalWithStart` (same Workflow ID, new Run ID) — normal, expected, not a crash. A reply-session's lifetime isn't bounded by any single Coordinator execution the way a subagent's child `TurnWorkflow` is genuinely bounded by its one parent turn (that *is* the right use of Temporal child workflows — see `components/temporal-workflow.md`); a reply-thread should keep existing and evolving regardless of whether the channel's main-session Coordinator happens to be alive, idle, or mid-restart at any given moment. So the relationship is expressed as durable Postgres data instead — same principle this project already applies elsewhere (session-filesystem leases, connection leases: durable relationships live in Postgres, never in worker-pinned or workflow-coupled state) — and each session, main or reply-scoped, stays a fully independent `CoordinatorWorkflow` with its own lifecycle exactly as today.

**Depth is inherent to the mechanism, not fixed by it.** `parent_session_key` chains support arbitrary depth for free — nothing enforces two levels. How deep a given platform's sessions actually nest is purely a consequence of that platform's own discriminator-resolution policy: Discord's (see `gateway/discord.md`) walks a reply chain all the way to its root and collapses every reply in that chain onto one session, which happens to produce a flat two-level tree (one main session, several flat sibling reply-sessions) — not a design limit, just that policy's own outcome. A platform wanting real multi-level branching would instead resolve `ParentSessionKey` to whatever session the *immediately*-replied-to message already belongs to (not the root), chaining one level deeper per reply-to-a-reply. Real cost difference worth naming, not glossed over: root-collapse needs no lookup at all (pure computation from message IDs the platform already hands over); immediate-parent resolution needs a reverse lookup (given a message ID, which session does it belong to) that root-collapse doesn't. Not built for any platform yet — deferred deliberately, not a gap to rediscover later.

**Genesis detection — free, from state already being written.** `submitMessageEvent`'s own `INSERT INTO sessions ... ON CONFLICT DO NOTHING` already runs for every message; checking whether that insert actually affected a row (mirroring the dedup insert's own existing `RowsAffected()` check, just applied to the other insert in the same function) tells you, for free, whether this is genuinely the first message this session_key has ever seen — exactly the one moment context injection needs to happen, no separate existence check needed.

**Context injection at genesis — copy the parent's current LCM state.** Implemented: `SeedChildSessionContextActivity` (`activities/activities/seed_child_session.py`), called from `coordinator.go` — `CoordinatorWorkflow` now takes an optional `ParentSessionKey` on `CoordinatorInput`, set by `submitMessageEvent` ONLY on the exact genesis condition above (never on a later message for an already-existing session, and never re-fires on this session's own future idle-timeout restarts, since the Gateway — not Temporal — is what decides whether to include it, gated on its own Postgres-level genesis check rather than any Temporal-level "did this call actually start the workflow" assumption). The activity calls `lcm.assemble()` (the exact function `ModelCall` itself uses — no separate/duplicated logic) against the parent session with an empty system prompt, strips the system-message wrapper `assemble()` always prepends, and writes the remainder — summary-DAG content and the parent's verbatim recent-message window, combined, exactly as `assemble()` already combines them for real inference — as a single new `kind='leaf'` `context_summaries` row for the child, `covers = '{}'` (a genesis snapshot references no specific message_ids under the child's own turn tree, since it has none yet). A genuine duplicate, not a shared reference: the child evolves independently afterward via its own future `compact()` calls, no mutable state shared between sessions. Live-verified directly against the real cluster using the real, already-running production session as the parent (16 real messages, real tool-call history) — confirmed the child got a real leaf summary containing that actual conversation content, correct `token_count`, empty `covers`.

If the parent has no real turn history yet (nothing has triggered it — plausible for a channel that's had only ambient chatter so far, see `gateway/discord.md`), `assemble()` naturally returns nothing to copy and the activity is a deliberate no-op (logged, not an error) — the child just starts with no injected context, same as before this feature existed. **Not resolved, deliberately deferred**: a copied LCM snapshot is "as of the parent's last real turn," which could be stale if ambient activity happened after that — blending in "ambient buffer since the parent's last real turn" alongside the copy, or accepting the gap as-is; small enough to defer without weakening the core mechanism, and genuinely platform-specific (Web has no ambient buffer at all) rather than something this generic activity should own.

### Resolved: Outbound Flow — Delivery as a Colocated/Separate Temporal Worker, Not a Broker
**Superseded design choice:** the original plan used a message broker (Redis Streams/NATS) as an "outbound queue" between the deliver activity and the gateway. Reconsidered and dropped — it solved a problem created by assuming delivery runs on a generic worker pool physically separate from the gateway. A simpler mechanism removes the need for a broker entirely:

- **The gateway process itself runs an embedded Temporal worker** (same binary/process as the HTTP server and every platform goroutine — not a separate Deployment), registered for `DeliverActivity` on a task queue specific to what it owns — but only while it actually holds the relevant connection lease (connection-based platforms) or unconditionally (webhook-based/polling platforms — see below).
- The turn workflow, at delivery time, computes the target task queue **deterministically** — `deliver:{platform}:{connection_id}` (no `{tenant}` component: each tenant already runs in its own Temporal namespace, which already scopes every task queue name to that tenant — including it again in the queue string would be pure redundancy). See "Resolved: Connection Leasing" below for what `{connection_id}` is and why it can't just be `{platform}` alone.
- `ExecuteActivity(DeliverActivity, response, { taskQueue: computed_queue })` — Temporal's own task dispatch delivers the activity task to whichever gateway replica is polling that queue. Retry, durability, and correct targeting all come from Temporal's existing activity mechanics — the same retry policy already defined in `components/activities-outbound-delivery.md`, no new system needed.
- The `delivered_responses` idempotency ledger (`components/activities-outbound-delivery.md`) is unchanged: the receiving replica still checks-then-inserts before making the real platform send call, regardless of how the activity task reached it.

**Platform-dependent worker registration — still a real, inherent split, now expressed as leasing rather than sharding:**
- **Webhook-based/polling platforms:** the embedded worker for these registers unconditionally on every replica — no coupling to any specific one, any replica polling the shared per-tenant-platform queue can serve it. For a polling platform specifically (`gateway/web.md`), delivery doesn't need this at all — see that doc's "delivery collapses" finding.
- **Connection-based platforms:** the send call must go out over the *exact* live socket, a physical constraint (a TCP connection is memory-resident to one process), not a design preference. So only the replica currently holding that connection's lease registers as the embedded worker for its `DeliverActivity` queue — dynamically starting/stopping worker registration as the lease is acquired/lost, not a static, always-on registration.

**A tenant can run more than one connection of the same platform kind** (e.g. two separate Discord bots, invited to different servers) — `{platform}` alone can't address delivery once that's true, since which specific bot connection has to send the reply is now ambiguous. `{connection_id}` closes that gap: a **stable identity for one specific connection, sourced from the platform itself, never invented in config**. For Discord's text connection this is the bot's own Discord user ID — the exact same id `discordMentionsUser` already reads via `session.State.User.ID` for mention detection, so nothing new is being introduced, just reused for addressing too. Resolving it doesn't require the live gateway/websocket connection to already be open: Discord's REST API answers `GET /users/@me` (in `discordgo`, `session.User("@me")`) using just the bot token, so each configured bot's `connection_id` is resolved once at startup — *before* that bot's connection lease is ever acquired, which matters because the lease itself needs to be keyed on `connection_id` (see below), not discovered only after a successful connect. A human-assigned name in Helm values was considered and rejected: it adds a field an operator has to invent and keep from colliding/drifting, for no benefit the platform's own real identity doesn't already give for free.

**`connection_id` doesn't have to be a single raw platform-issued id — it can be composed, as long as every component is still platform-provided.** A text connection is 1:1 with a bot (one gateway websocket per token), so the bot's own id is sufficient alone. That's not universal: `gateway/discord-voice.md`'s voice connections are 1:many per bot (the same bot can hold a simultaneous voice connection in every guild it's in), so a bot's id alone would collide across them — `{bot_id}:{voice_channel_id}` is still fully platform-provided (both halves come from Discord, nothing invented), just addressing a narrower thing: one specific live connection, not "this bot" as a whole.

### Superseded: Shard Assignment — Static Config + StatefulSet Identity
**2026-08-23.** This section assumed a shared, cross-tenant gateway potentially serving thousands of guilds, where Discord's own forced-sharding threshold (~2500 guilds) is a real concern. Under the corrected per-tenant deployment (above), each tenant's own Gateway process only ever serves *that tenant's own* guild(s) — essentially always far below any real sharding threshold. So the entire StatefulSet/shard-formula mechanism this section designed is very unlikely to ever be needed at all; replaced by a much smaller mechanism below. Kept here as history, not deleted.

### Resolved: Connection Leasing — A Postgres Lease Replaces Shard Assignment
For connection-based platforms only (webhook-based/polling platforms have no connection to arbitrate and need none of this). Reuses the exact renewal-based compare-and-swap lease pattern already built and proven for `session_filesystem_leases`/`leases.py` — a new lease *kind* on infrastructure that already exists, not a new mechanism:

```sql
CREATE TABLE gateway_connection_leases (
  platform      text NOT NULL,
  connection_id text NOT NULL,  -- Resolved: Outbound Flow — a platform-provided stable id
                                 -- (Discord: the bot's own user id via GET /users/@me),
                                 -- never invented in config. One row per connection this
                                 -- tenant runs, not one row per platform — a tenant running
                                 -- two Discord bots gets two rows here, both platform='discord'.
  holder_id     text NOT NULL,
  acquired_at   timestamptz NOT NULL DEFAULT now(),
  expires_at    timestamptz NOT NULL,  -- renewed periodically by whoever holds it, not a fixed TTL
  PRIMARY KEY (platform, connection_id)  -- tenant is implicit — this table lives in that tenant's own Postgres
);
```
- Each replica's connection-kind goroutine (one per configured connection — e.g. one per configured Discord bot token, not one per platform) tries to `acquire_or_renew` its own `(platform, connection_id)` row on its own heartbeat cadence, same `ON CONFLICT ... WHERE holder_id = $self OR expires_at < now()` semantics `leases.py` already uses. If it can't acquire (someone else holds it, not yet expired), it simply doesn't open a live connection this cycle and retries later — no error, no coordination protocol beyond the lease itself.
- If it acquires, it opens/maintains the real connection, renews the lease on the same cadence as its own reconnect/heartbeat logic, and registers as the embedded `DeliverActivity` worker for `deliver:{platform}:{connection_id}` for as long as it holds the lease. Registering under `connection_id` (not something tied to this specific lease acquisition or to `holder_id`) is what keeps the queue address stable across a reconnect or a handoff to a different replica — see "Resolved: Failure Analysis" below for why that stability is what makes a pending delivery survive either.
- Crash recovery: the lease simply expires (no renewal from a dead process) and the *next* replica to attempt acquisition gets it — no StatefulSet ordinal, no pinned identity, works identically whichever replica happens to pick it up, and the queue name it re-registers under is unchanged (it's a function of `connection_id`, not of who's holding the lease).

**Real consequence, worth being explicit about — this unlocks something the superseded shard design didn't have**: since ownership is now a lease, not a pinned ordinal, the Gateway can safely run as an ordinary, multi-replica `Deployment` even for connection-based platforms — every replica participates in lease acquisition, only one ever holds it at a time, and losing the holder just means another replica picks it up on the next attempt. No StatefulSet needed for any platform kind anymore.

**Why this and not "just don't worry about two connections"**: raised and checked directly — dedup (`ingested_messages`' own `PRIMARY KEY (platform, platform_message_id)`) already makes duplicate inbound *message processing* a non-issue for free, regardless of how many connections exist, race or not. The lease exists for a separate, real reason dedup doesn't touch: most connection-based platforms' own session semantics don't cleanly tolerate two simultaneous identifies with the same credential (Discord, for instance, can silently invalidate/flap sessions under this), and outbound delivery needs one unambiguous target to route to — a live socket Discord has already silently dropped isn't a valid delivery target even if a local replica still thinks it's connected.

### Resolved: Failure Analysis — What Happens When a Connection-Based Replica Dies Mid-Processing
Split into two genuinely different cases. Language updated 2026-08-23 from "shard"/"StatefulSet ordinal" to "lease" — the underlying two-case analysis is unchanged, only the mechanism restoring service after a crash is different (a lease reacquired by any replica, not a pinned ordinal resuming the same shard identity).

**1. Replica crashes before `SignalWithStart` succeeds (raw event received, not yet durably submitted).** This message is genuinely lost from our system's perspective — there is no mechanism that recovers it, because Temporal's durability guarantee only starts once something is inside Temporal, and nothing was written anywhere durable before the crash. This is an inherent boundary condition of bridging an external at-most-once event source into a durable internal system, not specific to this design. **What bounds the damage:** the platform's own protocol. Discord assigns each connection a sequence number and supports resuming a dropped session (replaying events since the last acknowledged sequence) rather than reconnecting cold. **Requirement this creates: gateway reconnection logic must prefer session-resume over fresh-reconnect wherever the platform supports it** — this is what actually closes most of the loss window, not anything in our own system.

**2. Replica crashes after `SignalWithStart` succeeds.** Fully covered by existing mechanisms, nothing new needed: the signal is durably in Temporal's workflow history regardless of the gateway replica's fate. The only consequence is a window where that connection has no active listener/delivery worker — inbound events during the gap follow case 1's story; outbound deliveries simply sit as pending Temporal activity tasks on `deliver:{platform}:{connection_id}` until some replica reacquires that `(platform, connection_id)` row in `gateway_connection_leases` and resumes polling — nothing is lost on that side, Temporal just holds the task, and it's still the same queue name waiting for it since that name is a function of `connection_id`, not of which replica or lease acquisition served it before the crash. The gap here is bounded by the lease's own expiry, a tunable, not by anything pod-identity-related.

**What does NOT help here, addressed directly:** writing the inbound message to Postgres at the gateway before signaling does not close the loss window — that write is just as vulnerable to a pre-submission crash as the signal itself, since both are downstream of the same gateway process being alive. The only thing genuinely upstream of a gateway crash is the platform's own durable record of what it already sent (the sequence-number/resume mechanism above) — which is also the reason the persist-to-Postgres step was moved off the gateway's inbound path entirely (see Inbound Flow, step 5) rather than kept as a redundant local safety net that doesn't actually add safety.

**Requirement this creates for `gateway_shard_state`** (name kept as-is for now — really "per-connection resume state" under the corrected topology, not literally shard-scoped anymore, and keyed the same way the lease table now is: `(platform, connection_id)` rather than `(platform, shard_id)`, since resume state is inherently per-connection once a tenant can run more than one — a rename is cosmetic, not resolved here): to make session-resume actually work across a crash/restart, the last-acknowledged sequence number needs to survive the crash. This is a few bytes, not real session state — stored in Postgres (`components/state-layer.md`), not on local pod disk, for the same reason session-filesystem rejected worker-pinned state. Whichever replica next acquires the connection lease reads this row to resume correctly — the lease and the resume-state row are two different concerns (who's allowed to connect right now vs. what sequence number to resume from), deliberately not merged into one row.

### Resolved: Voice Platforms — Cascaded Architecture, Not End-to-End Speech-to-Speech
Every voice-capable Gateway platform (Discord Voice first — `gateway/discord-voice.md` — intended as the reference shape for whichever comes after it) follows the same seven-stage cascaded pipeline, explicitly chosen over a single end-to-end speech-to-speech model:

```
Voice Client → Media Gateway → STT Service → Voice Orchestrator → LLM → TTS Service → Audio Player
```

**Rejected: a unified multimodal model taking audio in and producing audio out directly**, no discrete transcript/text stage in between. Considered and set aside, not on latency grounds (a well-tuned end-to-end model can be faster) but on reuse grounds: this project already has a complete, working, text-native orchestration layer — session/turn management, tool-calling, context assembly (LCM), memory — none of which speaks audio and all of which would need to be rebuilt or bypassed for a model that never produces an inspectable text representation mid-pipeline. Cascaded keeps every one of those components exactly as they are; voice only ever has to solve audio-in/audio-out at the edges.

**Stage-by-stage, generic → Discord-specific mapping** (`gateway/discord-voice.md` owns the Discord column in detail):

| Stage | Responsibility | Discord Voice mapping |
|---|---|---|
| **Voice Client** | Wherever the human's audio actually originates. Not something this project builds — it's the platform's own client. | Discord's own client app (desktop/mobile/web) — out of our control, out of scope. |
| **Media Gateway** | Jitter buffer, resampling, VAD/activity detection, **fast-path interruption (barge-in) detection** — audio-layer concerns, before anything is transcribed. | `discord_voice.go`'s capture loop + `voice_opus.go` (Opus decode) + `voice_vad.go` (VAD, `energyVAD` today). Fast-path barge-in is a real, currently-unbuilt gap — see below. |
| **STT Service** | Streaming transcription — partial transcripts as the utterance is still being spoken, a final transcript once it ends. | `voice_stt_tts.go`'s `transcribeAudio` — currently **batch**, not streaming (whole buffered utterance, one request, one final transcript, no partials). A real gap against this architecture, not yet resolved — see below. |
| **Voice Orchestrator** | Turn manager, conversation state, interruption/cancellation, context manager, tool execution. | Not new: this is exactly what `CoordinatorWorkflow`/`TurnWorkflow` already are, reused as-is — the reason voice was buildable at all without reinventing session/turn management. |
| **LLM** | Streaming tokens, tool calls, reasoning — ideally producing text incrementally, not as one atomic block. | `ModelCall` (`activities/activities/llm.py`) — currently non-streaming (returns one complete response). Real latency implication for voice specifically (see below); shared infra with every text platform, so changing it is real, cross-cutting scope, not voice-only. |
| **TTS Service** | Text → audio, streamed/chunked (sentence-level scheduling) so playback can start before the full response is ready. | `voice_stt_tts.go`'s `synthesizeSpeech` — currently **batch** (one call, the whole final message, one WAV back). Same shape gap as STT, mirrored on the output side. |
| **Audio Player** | Jitter buffer/playback, barge-in cancellation triggered by the Media Gateway's fast-path detection. | `deliver_voice.go`'s `OpusSend` loop — has both the slow path (a full transcript → a new turn → `turn.go`'s interrupt-race, "Resolved: Overlapping Speech / Interrupts") and, as of 2026-08-25, the fast path below. |

**Resolved: Fast-path barge-in (2026-08-25).** The slow path (VAD → utterance buffered → silence → transcribed → submitted as a `MessageEvent` → `SignalWithStart` → Coordinator forwards → `TurnWorkflow`'s interrupt-race cancels the activity) is correct for starting a *new turn* with the interrupting content, but far too slow to be the thing that stops the bot talking the instant a human starts. Closed with a purely local, in-process signal — `voice_bargein.go`'s `voiceBargeIn`, one instance per voice connection, shared by construction between the Media Gateway's capture loop (sender: fires on every VAD-classified speech frame, a no-op unless this connection is currently playing) and the Audio Player's send loop (receiver: selects on the returned channel alongside `ctx.Done()`/`OpusSend`, stops immediately on a signal). No Temporal, no Postgres, nothing durable — durability is exactly what makes the slow path too slow for this. The two paths are complementary, not alternatives: fast-path only ever stops local audio output; deciding what to do with whatever the human actually said is still entirely the slow path's job. Verified with real concurrency tests (`-race`, since two goroutines touch this by design) covering the no-playback no-op case, signal delivery during playback, repeated signals not blocking/panicking, a stale channel from an ended playback never receiving a signal meant for a later one, and a stress test of both sides cycling concurrently.

The cancellation mechanism the slow path already uses (`turn.go`'s `workflow.WithCancel` + a `Selector` racing a notify channel against the in-flight activity — "Resolved: Overlapping Speech / Interrupts," `gateway/discord-voice.md`) is the right *pattern* to extend if a future need requires cancelling further back than the delivery activity — an in-flight `ModelCall` or `ToolCall`, not only playback — once a real interrupt needs to abort generation itself. Not built — today cancellation only wraps delivery — but it's the same primitive (`workflow.Context`/`context.Context` cancellation, already pervasive in this codebase), not a new mechanism to invent when that need becomes real.

**One real, currently-unresolved gap remains** (not silently assumed to already work):

**Streaming STT/TTS, with pipelining as the actual point.** Was fully batch; now a real two-tier picture, verified directly against the live services (`whisper-svc`/`kokoro-svc`, both the `speaches` project) rather than assumed from documentation:
   - **Built (2026-08-25)**: `transcribeAudio` uses `/v1/audio/transcriptions`'s real `stream=true` (genuine SSE — confirmed multiple `data: {"text": ...}` events for a multi-sentence utterance, one per segment as the server decodes it) — lower time-to-result for a long utterance, though the full audio still has to be uploaded first (this streams the server's *output*, not the *input*; capture is still VAD-buffered-per-utterance, unchanged). `synthesizeSpeechPCM` requests raw PCM at Discord's exact sample rate directly (`sample_rate=48000`, verified via duration math to be honored exactly) and reads the response's own native `Transfer-Encoding: chunked` body incrementally — `deliver_voice.go` Opus-encodes and sends each frame as its bytes arrive rather than buffering the whole synthesized response first, real pipelining on the delivery side. Side effect: eliminated `ffmpeg` as a dependency entirely (`deploy/docker/gateway.Dockerfile`) — resampling was its only job, and the server now does that; only a trivial mono→stereo duplication remains, no external process needed.
   - **True bidirectional streaming (`AudioFrame → TranscriptDelta* → TurnFinalized` while the human is still actively speaking) — built 2026-08-26 (WhisperLive), audio feed gated 2026-08-29.** Real metrics showed the original *unconditional* feed pinning the deployment's single shared GPU at ~100% for the whole time the bot was joined and OOM-ing the node; the fix keeps streaming STT but feeds WhisperLive only while an utterance is in progress. Full detail in `gateway/discord-voice.md`'s "Resolved: Gated Streaming STT Feed" and its 2026-08-29 Notes Log. That endpoint turned out to be a full speech-to-speech agent session (its own persona, turn detection, response generation), not a transcription primitive — verified directly by probing it for real, not assumed. Resolved instead with **WhisperLive** (github.com/collabora/WhisperLive), a purpose-built streaming ASR project deployed as new GPU infra (`infra/model/whisperlive/`) — full detail, including two real bugs found and fixed against the live service, in `gateway/discord-voice.md`'s own "Resolved: True bidirectional realtime STT". No `pion/webrtc` in this codebase at all — WhisperLive's plain-WebSocket protocol needs no WebRTC client stack. Pipelining the LLM stage itself — `ModelCall` producing tokens incrementally, with a text segmenter flushing to delivery on real sentence boundaries (not naive period-splitting — `"Dr. Smith said..."` isn't one) — is a separate, cross-cutting piece (shared infra every text platform also calls), built and verified as of 2026-08-25 (see "Resolved: ModelCall Streaming" below) for Discord text; voice hasn't wired into it yet. Endpointing stays VAD's job either way, not STT's or the LLM's — verified directly: a two-utterance test confirmed Gateway's own VAD-driven utterance boundary can read a clean "what's new" answer off WhisperLive's own segment list, without WhisperLive's own internal VAD needing to be the authority on `TurnFinalized`.

### Resolved: ModelCall Streaming — Shared Infra, Text-First Rollout
**Built and verified (2026-08-25)** — scoped directly through conversation as deliberately cross-cutting: `ModelCall`, `TurnWorkflow`, `Deliver` are shared by every platform, so this was built and verified against **Discord text first** (no WebRTC transport in the mix), with voice reusing the same machinery once bidirectional STT exists, rather than a second, voice-only version. Two real design corrections happened during the build, both driven by re-reading the actual code rather than the docs-level sketch below — recorded as corrections, not silently absorbed into the description as if this were the plan all along.

**Scope, decided directly**: single-shot turns only — a turn that resolves in one `ModelCall` call with no tool calls. Why: whether a turn needs tool calls is only known once `ModelCall` finishes, but chunks must be delivered live, *during* the call, for streaming to have any latency benefit at all — there's no way to gate live dispatch on information that only exists after the call completes. The accepted, bounded edge case: a turn that streams content and then calls a tool anyway shows up as two messages (the streamed preview, then the real final answer once tool calls resolve), never a corrupted or wrong single message.

**Correction #1 — the reference-passing contract.** `turn.go`'s own doc comment is explicit: *"this workflow holds NO message content, tool arguments, or tool results in memory at any point — only IDs."* An earlier sketch of this feature signaled raw text chunks into the workflow, which would have violated that directly. Fixed: `ModelCall` writes each chunk's cumulative text to a new table, `turn_deliveries` (`(turn_id, seq)` primary key, `sent` boolean as its own idempotency guard — deliberately *not* folded into `delivered_responses`, which stays exactly as it was, one row per turn, since a chunk and a final message are different things with different identity shapes), and signals `TurnWorkflow` with only `{seq}` — turn_id is already the signaled workflow's own ID. The workflow relays that ID to a `DiscordDeliverChunk` activity, which reads the actual text from Postgres — the exact same "workflow holds IDs, activities hold content" shape every other piece of this codebase already uses, applied one level finer. This is why **`delivered_responses` needed no migration at all**, correcting the original sketch's assumption.

**Correction #2 — cumulative, not incremental, chunk content.** Discord's edit API replaces a message's whole content; `turn_deliveries.content` holds the running total through that chunk, not just its own increment, so `DiscordDeliverChunk` can always just `ChannelMessageEdit` with what it reads, no client-side concatenation.

- **Token relay**: `llm.call_model_streaming` (`activities/activities/llm.py`) calls the LLM with `stream=True` + `stream_options={"include_usage": True}`, accumulating content and (by index, per OpenAI's fragment-per-delta shape) tool-call deltas. `model_call.py`'s `ModelCallActivity` now takes a `temporal_client` (threaded from `tenant_worker.py`'s own `Client.connect()`) and uses it inside `_call_model_streaming_with_delivery` to signal the parent `TurnWorkflow` — an activity using its own client, distinct from the worker's task-polling connection, the documented way to push data to a workflow mid-flight.
- **Segmenter**: `activities/activities/sentence_segmenter.py`, real and independently verified — a sentence-ending punctuation mark followed by whitespace is a candidate boundary, suppressed after a known abbreviation or a single-letter "initial" (which incidentally also catches multi-period abbreviations' last segment, e.g. "U.S."). Verified directly against "Dr. Smith said hello. He left.", "The U.S. is large...", "J. K. Rowling wrote this...", and a decimal ("3.14") — none falsely split. Lives in Python (where the content actually is), not Go — the earlier docs-level sketch had it in `TurnWorkflow`, revised once the reference-passing contract fix (above) meant the workflow never sees the text to segment in the first place.
- **Discord text delivery UX**: create-once-then-edit-in-place, built as designed (`deliver_discord.go`'s new `DeliverChunk`, registered as `DiscordDeliverChunk` on the same embedded per-connection worker as `DiscordDeliver`) — first chunk creates the message and claims `turns.streamed_message_ref` (a new nullable column, the platform message being progressively edited — a plain opaque ID, same reference-passing treatment as everything else this schema stores), every later chunk edits it. `Deliver`'s end-of-turn call checks `streamed_message_ref`, but **not** the way originally described here — see the real bug fixed 2026-08-27 in the Notes Log: non-null does NOT reliably mean "this turn was already fully delivered via streaming," only that iteration 1 was streamed, which isn't the same thing once a tool call happens. No explicit rate-limit debounce logic was built — a real single-shot response is a handful of sentences, `discordgo`'s own client already waits out a 429 rather than dropping the request, and this project's own "don't build for a decision without real usage data" discipline argues against speculative debounce machinery; a real gap to revisit if actual usage shows edits bunching tightly enough to matter.
- **Reliability — activity retries after partial output is visible**, built exactly as designed: `activity.heartbeat(seq)` is called only *after* a chunk is durably written and signaled (ordering is what makes the check meaningful). On entry, `_call_model_streaming_with_delivery` checks `activity.info().heartbeat_details` — non-empty means an earlier attempt already streamed real, visible output; raises `ApplicationError(..., non_retryable=True)` rather than silently regenerating, since the underlying LLM call can't resume and isn't guaranteed to reproduce the same content. `TurnWorkflow` then treats this turn's generation as genuinely failed, same as any other unrecoverable `ModelCall` failure — no special silently-papered-over case.
- **Gated on platform, not just iteration**: `model_call.py` checks `sessions.platform` against the set of platforms `turn.go` actually knows how to stream to — Discord text originally, widened 2026-08-26 to include Discord voice (`gateway/discord-voice.md`'s own Notes Log has the voice-specific detail: `VoiceDeliverChunk`, the delta-from-cumulative computation, barge-in interaction). Streaming unconditionally for every platform's first call would just be wasted `turn_deliveries` writes and a signal nobody ever receives. Caught by re-reading the code after the first real integration test passed, not assumed correct from the design.
- **Race closed 2026-08-26, discovered while extending this to voice**: the original chunk-relay coroutine ran as a detached `workflow.Go`, with nothing forcing `TurnWorkflow`'s main loop to wait for it before proceeding to end-of-turn `Deliver`. Every chunk signal (including the forced final flush) is provably recorded in this workflow's history no later than `ModelCall`'s own completion event (`model_call.py`'s `on_chunk` awaits each signal RPC before returning), but that only guarantees the signal has *arrived* — not that the detached goroutine had actually *finished dispatching and awaiting* the corresponding delivery activity before the workflow moved on. For text this was a narrow, easy-to-miss gap (a lost final edit, invisible since nothing re-checks it); for voice it would have meant occasionally-silently-dropped audio for the last sentence of a response, on every turn that happened to lose the race — unacceptable for something meant to ship as a real feature, not deferred as a known text-only imperfection. Fixed by replacing the plain `mcFuture.Get()` + detached goroutine with `turn.go`'s new `awaitModelCallWithStreaming`: one `workflow.Selector` loop merging `ModelCall`'s own completion with the chunk signal channel, so the loop cannot exit until `ModelCall` is done, plus a final non-blocking drain (`ReceiveAsync`) after the loop for the case where `Select` happened to fire the completion case first in the same history batch a trailing chunk signal arrived in. Applies to both platforms — Discord text is now provably race-free too, not just voice.
- **Real verification, not just shape-checking**: the segmenter was checked against real tricky text directly (above). A full local integration stack was stood up to verify everything else for real — a disposable local Postgres (Docker, all 6 migrations applied), a local Temporal dev server (`temporal server start-dev`), the real, unmodified `loop-worker` and `tenant-worker` binaries, and a fake `DiscordDeliver`/`DiscordDeliverChunk` embedded worker (the one piece that genuinely can't be exercised without a live Discord bot + channel) standing in for the real one. The one thing truly unreachable from this environment — a real commercial LLM provider (the cluster's own `litellm-config` has no chat-completion model registered, only embeddings/STT/TTS) — was stood in for by a small local mock server speaking real OpenAI-compatible SSE, deliberately scripted with the exact "Dr. Smith"/"U.S."/"J. K." text the segmenter was already checked against, so the whole pipeline's handling of it could be verified together, not just the segmenter in isolation. A real `TurnWorkflow` execution against all of this produced exactly 3 correctly-ordered `DiscordDeliverChunk` calls with correct cumulative content, a correctly-skipped final `Deliver` (confirmed via a genuine bug in the *test double* itself, caught and fixed before trusting the result — the first run showed a duplicate final send because the fake activity hadn't been made to replicate `streamed_message_ref`-setting), and a real `ActivityEnvironment`-based test separately confirmed the retry-safety check fires on injected heartbeat details and does not fire on a genuine first attempt. All test infrastructure (containers, dev server, mock server, a throwaway `cmd/teststreaming` driver) torn down after.
- **Not yet designed**: how streamed tool-call deltas interleave with streamed content deltas in the cases this pass's single-shot scope doesn't cover — deliberately out of scope, not silently dropped.
- **Not yet done**: no real Discord bot/channel test (same category of gap as every other Gateway feature in this doc) — `go build`/`go vet`/`gofmt` and Python `py_compile` all clean on the whole affected surface.

### Resolved: Media Plane / Agent Plane Separation
Not new work — an explicit naming of a separation this project's architecture already has, for an unrelated reason, well before voice was ever discussed. The **Media Plane** (microphone/speaker, audio frames, VAD, jitter, codec, playback, barge-in) is the Gateway process — per-tenant, holds live platform connections (`multi-tenancy.md`'s credential-isolation principle is exactly why it's a separate process per tenant in the first place). The **Agent Plane** (conversation, memory, LLM, tools, planning, context, permissions) is `loop-worker` (the shared, tenant-agnostic workflow engine — `CoordinatorWorkflow`/`TurnWorkflow`) plus `tenant-worker` (per-tenant activities — `ModelCall`, `ToolCall`, memory writes). The Gateway never touches conversation content beyond passing IDs and raw inbound/outbound audio or text; `loop-worker`/`tenant-worker` never touch a live platform connection at all. This is what let voice get built without inventing a new orchestration layer — the Agent Plane was already platform-agnostic by construction (the whole reason `MessageEvent` exists), so a new Media Plane (Discord Voice's capture/VAD/STT/TTS/playback code) could be added without touching it.

**Audio transport is a Media Plane concern, platform-dependent, not a project-wide choice.** For Discord specifically this question doesn't arise — Discord dictates its own voice transport (RTP/UDP via `discordgo`, mediated entirely by Discord's own servers); there's no WebSocket-vs-WebRTC decision to make for a platform that already picked its own protocol before we ever connect to it. It becomes a real decision only for a *future* voice platform this project builds a client for itself (browser voice for `agent-web`, a phone-call platform) — there, WebRTC (jitter handling, codec negotiation, NAT traversal built in) is the right default over a raw WebSocket for realtime audio, a plain prototyping shortcut aside. Recorded here as a standing principle for whichever platform needs that decision, not something Discord Voice needs to act on.

### Key Design Decisions (recap)
- **One Gateway process per tenant** (Go, `agent-harness-tenant` chart), not shared across tenants — required by `multi-tenancy.md`'s credential-isolation principle, which the original "shared per platform" framing never satisfied. Every platform kind that tenant uses runs inside this one process, as separate goroutines — not one process per (tenant × platform).
- Always async — no platform requiring a same-request synchronous response is supported.
- Delivery is a Temporal worker (embedded in the gateway process), addressed via deterministic task-queue routing (`deliver:{platform}:{connection_id}`, no `{tenant}` — already implied by the tenant's own Temporal namespace) — not a message broker.
- A tenant can run more than one connection of the same platform kind (e.g. two Discord bots) — addressing is per-*connection*, not per-platform. `connection_id` is always sourced from the platform itself (Discord: the bot's own user id, resolved via `GET /users/@me` before the connection lease is even acquired), never an operator-invented name.
- Connection-based platforms: a Postgres-backed connection lease (reusing `session_filesystem_leases`/`leases.py`'s pattern), keyed `(platform, connection_id)`, determines which replica holds a given live connection and registers as that connection's embedded delivery worker — an ordinary multi-replica `Deployment`, no StatefulSet/shard formula needed at per-tenant scale.
- Webhook-based/polling platforms: every replica's embedded worker registers unconditionally; no lease, no connection-identity concept. Delivery for a polling platform specifically may not need this mechanism at all — see `gateway/web.md`.
- Inbound message loss (pre-signal crash) is real and platform-resume-bounded, not solvable by adding a redundant Postgres write at the gateway.

### Open Questions / To Design
**Shared/index-level (genuinely cross-platform, belongs here):**
- Exact connection-lease heartbeat/renewal cadence — same numeric-tuning discipline as every other undecided threshold in this project (heartbeat intervals, compression thresholds), deliberately not sized here pending real usage data.
- Whether `gateway_shard_state` gets renamed to something that doesn't say "shard" now that shard assignment is superseded — its key structure is now settled (`(platform, connection_id)`, matching `gateway_connection_leases`), just the literal table/column names still say "shard"; the rename itself remains cosmetic, not resolved.
- **Streaming STT/TTS provider strategy** — same section: whether stage 3/6 genuinely need a different provider (a purpose-built streaming ASR, an LLM that streams tokens) or can be approximated against the currently-integrated batch services (Whisper, Kokoro). Not resolved; real usage on the currently-batch pipeline should inform whether this is worth the added complexity at all.

**Platform-specific (moved out — belongs in each `gateway/<kind>.md`, not here):**
- Auth model — inherently per-platform (Clerk for Web, resolved in `gateway/web.md`; a real third-party platform would need its own bot-token/OAuth story, not a shared mechanism).
- Slash command routing (`/approve`, `/deny`, `/stop`) — a Discord/Slack-specific UI concept, not relevant until a platform with slash commands is actually being built.
- Connection/reconnection handling beyond the general resume-preference requirement below — genuinely per-platform (Web has none, since it polls; a connection-based platform would need real backoff/resume-API specifics).

### Notes Log

- 2026-08-30: **Gateway binary refactored from one flat `package main` into `internal/gateway/{core,lease,speech,web,discord,discordvoice,discordui}`** — see "Resolved: Package Structure" above for the full account (the dependency DAG, why no `Platform` interface, the god-struct split). ~34 files / ~6,200 lines moved; every `git mv` recorded as a rename so history follows. `core` is the shared inbound seam (`MessageEvent` + `Ingest` + `SessionKeyFor`), compiler-forbidden from importing any platform adapter. Behavior is unchanged — `go build` (incl. CGO), `go vet`, `gofmt`, `go test` all green, and a new `core` unit test pins `SessionKeyFor` / `DiscordThreadRootFromSessionKey` round-trips. Done in one pass at the user's direction.
- 2026-08-29: **Real, live bug: a streamed Discord-text chunk that's non-empty but pure whitespace permanently wedges the whole turn workflow — found while testing an unrelated feature, root-caused via real Temporal history, fixed, then revised per direct feedback.** Surfaced live while testing the mid-turn approval flow: a real turn's gated `shell_exec` call never produced an approval prompt at all. `temporal workflow describe` showed the actual cause — not the approval mechanism, something upstream of it: a `DiscordDeliverChunk` activity stuck on attempt 10, retrying forever with `HTTP 400: Cannot send an empty message` from Discord's own API. Confirmed in Postgres: `turn_deliveries` seq 1's content was 3 bytes of pure whitespace — a real chunk the sentence segmenter legitimately flushed, non-empty at the Python string level, but functionally empty to Discord. Because `deliverChunk` (`turn.go`) never set a `RetryPolicy` on this activity, a permanent failure retried forever instead of giving up — and because that call blocks the same `Selector` case that owns `ModelCall`'s own already-completed result, the entire `TurnWorkflow` was wedged before it ever reached the tool-call fan-out that would create the `user_input_requests` row. `VoiceDeliverChunk` (`deliver_voice.go`) already guards against this exact shape (an empty-after-sanitize check); `DiscordDeliverChunk` never got the text-side equivalent.
  **First fix (same day): silently skip the send when content trims to empty**, mirroring `Deliver`'s own existing `content == ""` skip. **Reverted the same day, asked directly**: "sometimes the space can also be a genuine stream chunk... shouldn't we send back spaces too and treat them like a string?" — correct pushback. Discord's rejection of whitespace-only content is a real, external platform constraint (confirmed live, not a policy choice), but treating "empty" as "nothing to do" is the wrong generalization to bake into the shared path: other platforms may not share this rejection at all, and silently no-op'ing makes a real (if content-thin) turn look like nothing happened. **Revised fix**: `discordSendableContent` (`deliver_discord.go`) substitutes a visible `"(empty)"` placeholder, applied ONLY immediately before the real `ChannelMessageSend`/`ChannelMessageEdit` calls in both `Deliver` and `DeliverChunk` — never used to decide whether to send. The model's real content in `messages`/`turn_deliveries` is untouched; only what's actually handed to Discord's API is substituted, and only when it's genuinely empty/whitespace — real content, including meaningful leading/trailing whitespace, passes through byte-for-byte unchanged. Same platform-boundary-only pattern `sanitizeForSpeech` already uses on the voice side, just scoped correctly to Discord's own constraint this time rather than assumed universal.
  **Independently, still kept**: `turn.go`'s `deliverChunk` now sets `RetryPolicy: &temporal.RetryPolicy{MaximumAttempts: 3}` on the chunk-delivery activity — real defense-in-depth, orthogonal to the content fix. Even with whitespace now handled, some OTHER permanent Discord API rejection (a deleted channel, a permissions change, some future edge case) should still fail bounded rather than wedge the workflow forever the way this one did — matches the closure's own stated intent ("a dropped streamed preview chunk isn't fatal"), which the missing retry cap had never actually matched.
  Verified with a real unit test against `discordSendableContent` (whitespace/empty → placeholder, real content with meaningful surrounding whitespace → byte-for-byte unchanged), deleted after passing. The originally-wedged turn (`agent:main:discord:channel:1504889591667359784:turn:9`) was terminated via `temporal workflow terminate` to stop its unbounded retry loop — left `turns.status`/`tool_calls.status` orphaned at `running`/`pending` in Postgres (expected fallout of a hard terminate, not a further bug; harmless, doesn't block a new turn). `go build`/`go vet`/`gofmt` clean on the whole `workflows` module. Not yet re-verified against a fresh live approval-flow test — needs `loop-worker` (the shared `agent-harness-shared`/`harness` release, not the per-tenant chart) rebuilt and redeployed, since this fix lives entirely in shared workflow code.
- 2026-08-27 (latest): **Real, live bug: the "accepted edge case" this feature's own design explicitly called for (two messages, not one, when a streamed turn calls a tool anyway — line 178 above) was never actually built that way, and the real final answer was silently dropped, on both platforms.** Found from a real user report of two shapes of the same symptom: a text conversation where the bot said "let me check what tools are available" and then never followed up until the user pinged again (at which point the SAME already-computed answer surfaced, read from `messages` by the follow-up turn), and voice reports of filler-adjacent phrases playing with no real response ever following. Traced via a real Temporal workflow history read (`temporal workflow show --output json`): the turn showed a genuine `WORKFLOW_TASK_FAILED` (`UnhandledCommand`) that self-recovered on retry, two real `ToolCall` activities, three real `ModelCall` iterations, and a `Deliver`+`DiscordDeliver` pair that both reported `ACTIVITY_TASK_COMPLETED` — the activity genuinely believed it succeeded, because it had: `DiscordDeliver`'s own `streamedMessageRef != nil` check treated "iteration 1 was streamed" as "the whole turn was streamed," true only when the streamed iteration made no tool call. `turn.go`'s `streamingEligible` gate is `iterations == 1`, with no way to know in advance whether that iteration will call a tool — so any turn that streams its first content and then calls a tool produces a real final answer in a LATER `messages` row that was never streamed at all, and the old check discarded it outright instead of sending it as the "second message" this section's own scope decision (line 178) already said it should.
  Fixed identically on both platforms, by comparing rather than trusting the flag: `DiscordDeliver` now compares the turn's real latest assistant message against `turn_deliveries`' own last cumulative row — equal means iteration 1 really was the whole turn (skip, matches the original intent), different means real unsent content exists, sent as a follow-up `ChannelMessageSend` (not an edit — it's a genuinely separate later thought, matching the two-message shape this section always said was correct). `deliver_voice.go`'s `voice_streamed` check gets the same comparison, falling through to the existing synthesize-and-play path (speaking the real final content) instead of unconditionally skipping. `go build`/`go vet`/`gofmt` clean on the whole module. Not yet re-verified live against a fresh deploy — the diagnosis came from reading a real, already-completed turn's Temporal history, not from re-running the fixed code yet.
- 2026-08-26 (latest): **ModelCall streaming extended to Discord voice, and a real race closed in the process** — prompted directly by asking whether Temporal's new Workflow Streams feature (Signal/Update/Query-based durable pub/sub, ~2s default batch interval) could reduce voice latency. It couldn't help the actually latency-critical parts (frame-level barge-in, already deliberately kept off Temporal entirely — see fast-path barge-in above), but the question surfaced that ModelCall streaming, already shipped for Discord text, had never been wired into voice at all: voice was still waiting for a turn's complete LLM response before `VoiceDeliver` started any synthesis, the single biggest remaining latency cost in the pipeline.
  `model_call.py`'s platform gate widened from `platform == "discord"` to `platform in ("discord", "discord-voice")` — `llm.py`'s `call_model_streaming` needed no change at all, already fully platform-agnostic. New migration `007_voice_streaming_delivery.sql` (`turns.voice_streamed boolean`) — voice's equivalent of `streamed_message_ref`, a plain flag rather than a text ref since there's no "message" object for continuous audio to address. New `deliver_voice_chunk.go` (`voiceDeliverActivity.DeliverChunk`, registered `VoiceDeliverChunk`): since `turn_deliveries.content` is cumulative (Discord text's own edit-API requirement, left unchanged), this computes the DELTA between consecutive rows — the actual new sentence(s) — and synthesizes only that, never the whole cumulative text again. Returns `(interrupted bool, error)`, not a plain error like `DiscordDeliverChunk`: `turn.go` needs to know when a chunk's playback was stopped by the fast-path barge-in so it can stop dispatching this turn's remaining chunks rather than talking over the human a second time. `deliver_voice.go`'s frame-conversion/Opus-send loop extracted into a shared `streamPCMToOpus`, used by both `Deliver` and `DeliverChunk`. `Deliver`'s own end-of-turn call now checks `voice_streamed` (mirroring `DiscordDeliver`'s `streamed_message_ref` check) to skip re-synthesizing and replaying the whole response when it was already delivered progressively.
  **Real race found and closed while doing this, not before**: re-reading the original text-only chunk-relay coroutine (a detached `workflow.Go`) to extend it to voice surfaced that nothing ever forced `TurnWorkflow`'s main loop to wait for it before proceeding — `model_call.py`'s `on_chunk` awaits every signal (including the forced final flush) before returning, so every chunk is provably recorded in history no later than `ModelCall`'s own completion event, but that only guarantees the signal *arrived*, not that the detached goroutine had *finished delivering it*. For text this was a narrow, invisible gap (a possible lost final edit); for voice, shipping it unfixed would have meant occasionally, silently dropped audio for a response's last sentence — not acceptable for a real feature. Closed by replacing the plain `.Get()` + detached goroutine with `turn.go`'s new `awaitModelCallWithStreaming`: a `workflow.Selector` loop merging `ModelCall`'s completion with the chunk signal channel, so the loop can't exit until `ModelCall` is done — plus a final non-blocking `ReceiveAsync` drain after the loop specifically for the case where `Select` fires the completion case first when both became ready in the same history batch (caught by working through the Selector's actual one-case-per-call semantics on paper, not by testing it — an easy way to silently reintroduce the exact bug this was meant to fix). This closes the race for **both** platforms — Discord text is now provably race-free too, not just voice, a genuine improvement to already-shipped code, not scope creep for its own sake.
  `go build`/`go vet`/`gofmt` all clean on the whole `workflows` module; Python `py_compile` clean on the two edited files. **Not yet verified against a real live Discord voice channel** — same category of gap as every other pipeline change in this doc before its first live test; needs a gateway image rebuild + `helm upgrade` before it takes effect.
- 2026-08-25 (superseding the entry below): **ModelCall streaming built and verified.** New migration (`006_streaming_delivery.sql`: `turn_deliveries` table, `turns.streamed_message_ref` column), `activities/activities/sentence_segmenter.py` (new), `llm.py`'s `call_model_streaming`, `model_call.py`'s `_call_model_streaming_with_delivery` (Temporal client threaded in from `tenant_worker.py`), `turn.go`'s new chunk-relay coroutine, `deliver_discord.go`'s new `DeliverChunk`/`DiscordDeliverChunk`. Two real corrections surfaced during the build, not before it: signaling raw content into the workflow would have violated `turn.go`'s own documented reference-passing contract (fixed — signal carries only a chunk's `seq`, content lives in the new `turn_deliveries` table, read by ID like everything else), and cumulative (not incremental) chunk content is what Discord's replace-only edit API actually needs. A real, disposable local integration stack (Postgres in Docker, a local Temporal dev server, the real unmodified `loop-worker`/`tenant-worker` binaries, a mock OpenAI-compatible SSE server standing in for the one genuinely unreachable dependency — a real commercial LLM, since the cluster's own litellm config has no chat model registered) caught a real bug in the test's own fake Discord activity before trusting a green result, then confirmed the whole pipeline end-to-end: correctly-ordered chunk delivery, correct cumulative content, the final `Deliver` correctly skipping a duplicate send. A separate `ActivityEnvironment`-based test confirmed the fail-loud retry-safety check fires and doesn't false-fire. All temporary infrastructure torn down after. Full detail, including the "single-shot only" scope decision and its rare accepted edge case (two messages instead of one if a turn streams then calls a tool anyway), in "Resolved: ModelCall Streaming" above.
- 2026-08-25 (superseded by the entry above — kept for the record of what was decided before the build surfaced the two corrections): **ModelCall streaming scoped and designed** (paired with `gateway/discord-voice.md`'s bidirectional-STT session-model design — the two were planned together since they only really pay off in combination for voice, per "Resolved: ModelCall Streaming" above). Real forks worked through directly: token relay (activity signals workflow via its own Temporal client), segmenter placement (`TurnWorkflow`, deterministic), Discord text delivery UX (edit-in-place, chosen and confirmed knowing it changes the live text pipeline, not just voice), and a reliability gap caught before it would have shipped — a retry mid-stream isn't safe once output is already visible, resolved via Activity Heartbeats + fail-loudly-on-retry-after-partial-output rather than a "last edit wins" band-aid. Design only, no code yet — `docs/components/gateway/discord-voice.md`'s own Notes Log has the paired bidirectional-STT decisions.
- 2026-08-25 (later than fast-path barge-in): **Turn-lifecycle fork decided: Gateway-local**, not a shared `turns.status` schema change — see `gateway/discord-voice.md`'s own Notes Log for the decision detail and a real implementation wrinkle it surfaced (the `GENERATING` state spans a process boundary — `tenant-worker`, not the Gateway — that "keep it local" doesn't automatically solve). Design only, not built.
- 2026-08-25 (even later still, after tier 1): **Fast-path barge-in built** — the one item from an Open Questions triage that was actually ready to build immediately (no new dependency, no product decision pending, unlike Silero VAD/`/v1/realtime`/the Turn-lifecycle fork). `workflows/cmd/gateway/voice_bargein.go` (new): a purely local, in-process signal (`voiceBargeIn`) shared between `voiceCaptureLoop` (sender — fires on every VAD-classified speech frame) and `voiceDeliverActivity.Deliver` (receiver — selects on it alongside `ctx.Done()`/`OpusSend`, stops immediately, returns `nil` since a barge-in stop is expected behavior, not a failure). No Temporal, no Postgres — deliberately, since durability is exactly what makes the slow path (above) too slow for this. Verified with real `-race` concurrency tests (deleted after): no-playback no-op, signal delivery mid-playback, a stale channel from an ended playback never receiving a signal meant for a later one, and a 200-cycle stress test running both sides concurrently — all clean. `gateway/discord-voice.md`'s own Notes Log has the Discord-specific wiring detail.
- 2026-08-25 (even later still): **Streaming STT/TTS tier 1 built and verified against the real live services.** Scoping "which streaming STT provider" led to actually checking `whisper-svc`/`kokoro-svc`'s real capabilities (port-forwarded and inspected their OpenAPI schemas + real requests) rather than assuming a new provider was needed — found `/v1/audio/transcriptions` genuinely supports `stream=true` (real SSE, confirmed with actual synthesized speech via macOS `say`, piped through both services for a genuine round trip) and `/v1/audio/speech` genuinely honors an explicit `sample_rate`. Neither needed a new external provider. `voice_stt_tts.go` rewritten: `transcribeAudio` now parses real SSE segments; `synthesizeSpeechPCM` (replacing `synthesizeSpeech`) requests 48kHz mono PCM directly and returns the still-open response body for incremental consumption. `deliver_voice.go` reads and Opus-encodes frame-by-frame as bytes arrive instead of buffering the whole response. `voice_convert.go`'s `ffmpeg`-based `convertToVoicePCM` deleted entirely, replaced by `monoToStereoPCM` (pure Go, no subprocess) — `deploy/docker/gateway.Dockerfile`'s `ffmpeg` package dropped, partially reversing the attack-surface tradeoff that Dockerfile rewrite made, now that resampling is the server's job. Verified end-to-end with two throwaway tests (deleted after) against the real services: a full TTS→Opus-encode pipeline (110 real frames from real synthesized speech), and a genuine round trip — synthesize a sentence via Kokoro, pipe the resulting audio back through Whisper, get back a transcript matching the original almost verbatim. One transient failure on a first cold run turned out to be a network flake, not a bug — confirmed by two clean consecutive passes afterward. True bidirectional realtime streaming (`/v1/realtime`, confirmed to exist but not used) and LLM token streaming remain the deferred, harder tier — not touched in this pass.
- 2026-08-25 (much later still): **Deepened the cascaded architecture with a real production reference** (a detailed writeup covering pipelining, streaming STT contract shape, endpointing, Turn-as-first-class-object, barge-in cancellation propagation, LLM-to-TTS sentence segmentation, WebRTC, and Media/Agent Plane separation), evaluated point-by-point against what this project already has rather than adopted wholesale. Net result: two points were free — Media Plane/Agent Plane separation is already this project's architecture (`loop-worker`+`tenant-worker` = Agent Plane, Gateway = Media Plane), predating voice entirely, for `multi-tenancy.md`'s credential-isolation reason, not a voice one; the cancellation-token pattern being recommended is already in use (`workflow.Context`/`context.Context`, `deliverConnectionBased`'s Selector-race), just not yet extended past the delivery activity. Five points (pipelining, streaming STT deltas, don't-call-the-LLM-per-delta endpointing, LLM-to-TTS sentence chunking, the "Dr. Smith" segmentation nuance) turned out to all be depth on the *same* already-open "streaming STT/TTS" gap, not five new items — folded into that gap's own entry above rather than tracked separately. One point (WebRTC vs. WebSocket) doesn't apply to Discord Voice at all — Discord dictates its own transport — but is real and worth keeping as a standing principle for whichever future voice platform this project builds a client for itself. One point (Turn as a first-class object with a rich lifecycle) is real, voice-specific, and deliberately left as an open fork rather than resolved unilaterally — see `gateway/discord-voice.md`'s own Notes Log for why (the shared `turns` table's coarse status vs. a Gateway-local richer state machine).
- 2026-08-25 (much later): **Cascaded architecture locked in explicitly**, as a standing decision for every future voice platform, not just Discord — prompted directly ("we're building support for cascaded architecture... I want to stick to using this architecture") with a reference diagram (Voice Client → Media Gateway → STT Service → Voice Orchestrator → LLM → TTS Service → Audio Player). New "Resolved: Voice Platforms — Cascaded Architecture" section above: the rejected alternative (a unified speech-to-speech model) and why (reuse of the existing text-native orchestration layer, not a latency argument), the seven-stage table mapping generic responsibilities onto what's already built for Discord Voice, and two real gaps this framing surfaced that hadn't been named before — fast-path barge-in (today's interrupt mechanism only fires after a full transcript, far too slow for natural conversational stop-talking latency) and streaming STT/TTS (both currently whole-utterance/whole-response batch calls, not the partial/incremental shape the architecture calls for). Neither gap is resolved here — named and added to Open Questions, not silently built around.
- 2026-08-25 (yet later): **Voice gateway design opened** — `gateway/discord-voice.md` (new), scoped through direct conversation to Discord voice first, intended as the first of possibly several voice-capable platforms (mirrors Discord being the first connection-based text platform). One generalization landed here rather than in the platform-specific doc since it changes a shared mechanism: `connection_id` (the "Resolved: Outbound Flow"/"Resolved: Connection Leasing" addressing this doc already has) now explicitly allows a *composed* identity, not only a single raw platform-issued one — text's 1:1-per-bot connection made a bare bot id sufficient, but a voice connection is 1:many-per-bot (the same bot can hold a simultaneous voice connection in every guild it's in), so `{bot_id}:{voice_channel_id}` is what actually addresses one specific connection. Design only — see `gateway/discord-voice.md`'s own Notes Log for the rest.
- 2026-08-25 (later): **Outbound delivery built for Discord** — implements the corrected addressing from the entry below. `workflows/cmd/gateway/deliver_discord.go` (new): `discordDeliverActivity.Deliver` — checks-then-inserts `delivered_responses` (idempotency, unchanged from `activities-outbound-delivery.md`), reads the turn's final assistant content and its session's `channel_id` via one join (`turns.parent_id` IS the session_key for a top-level turn — no new column needed to make that link), sends via the live `*discordgo.Session`. `discord.go` restructured: `connectionID` (the bot's own Discord user id) resolved once via `dg.User("@me")` before the lease loop even starts, since the lease is now keyed on it; the same `discordgo.Session` is reused across reconnect attempts instead of rebuilt each time; `runDiscordConnection` now also creates, starts, and (deferred) stops a `worker.New(...)` registered for `"DiscordDeliver"` on `deliver:discord:{connectionID}`, scoped to exactly the lifetime of holding the lease. `leases.go` widened to the `(platform, connectionID)` signature the migration below defines. Threading `connectionID` to the turn workflow turned out not to need a lookup activity at all: `CoordinatorWorkflow` already re-constructs `TurnInput` fresh for every turn from its own `CoordinatorInput`-derived local state (the same mechanism `SessionKey` itself already uses), so `ConnectionID` rides along for free — added to `types.TurnInput`/`CoordinatorInput`/`MessageEvent`, set unconditionally (not gated to genesis, unlike `ParentSessionKey`) so both a session's real first message and an ordinary idle-timeout coordinator restart both re-supply it correctly. `turn.go` dispatches the new `DiscordDeliver` activity as a pure addition alongside the existing Python `Deliver` call at all 4 of `TurnWorkflow`'s delivery sites (3 in `failTurn`, 1 at normal completion) — gated on `platformFromSessionKey`/`isConnectionBasedPlatform` (a literal `platform == "discord"` check, not a registry yet) and a non-empty `ConnectionID`, so Web's existing path is completely untouched. Migration `005_delivery_routing.sql`: widens `gateway_connection_leases` from `PRIMARY KEY (platform)` to `PRIMARY KEY (platform, connection_id)`; also copied `004_multi_session_channels.sql` (previously applied to the real cluster out-of-band and never recorded) into the Helm chart's `files/` alongside it, and backfilled its `schema_migrations` row to match reality, closing a real gap that would otherwise have broken this migration on the standard deploy path. Go build/vet/gofmt all clean. **Not yet verified against a real Discord send** — needs an actual image rebuild/redeploy and a live @mention to confirm end-to-end (this tenant already has ingestion/mention-detection/turn-dispatch confirmed live per `gateway/discord.md`'s own Notes Log; only the send itself is unverified).
- 2026-08-25: **Delivery addressing corrected before any of it was built** — prompted by starting to implement "Resolved: Outbound Flow" for Discord and being asked directly whether the planned `deliver:{tenant}:{platform}` naming actually held up. It didn't, for two independent reasons surfaced through direct conversation, not found by testing: (1) `{tenant}` is pure redundancy — every tenant already runs in its own Temporal namespace, which already scopes every task queue name to that tenant, so writing it into the queue string a second time added nothing; (2) `{platform}` alone silently assumed at most one connection per platform per tenant, which doesn't hold — a tenant can run more than one Discord bot (different servers), and each needs its own delivery target since a reply has to go out over the specific bot's own live socket, not just "some Discord connection this tenant has." Resolved: `deliver:{platform}:{connection_id}`, where `connection_id` is a platform-provided stable identity, never operator-invented — for Discord, the bot's own Discord user id, resolved via a plain REST call (`GET /users/@me`, no live gateway connection needed) *before* that connection's lease is ever acquired, since the lease itself now needs to be keyed on `connection_id` too (`gateway_connection_leases` widened from `PRIMARY KEY (platform)` to `PRIMARY KEY (platform, connection_id)`). The key property this preserves, checked explicitly against the reconnect/replica-handoff case: the queue name is a pure function of `connection_id`, never of `holder_id` or of which lease *acquisition* is currently live — so a pending delivery task for a connection that just dropped and reconnected (possibly on a different replica) is still sitting on the exact queue name the next acquirer registers under, with nothing to reroute. Design only — `gateway_connection_leases`/`gateway_shard_state` still exist in their original single-connection-per-platform shape in the actual schema; the embedded Temporal worker, the widened lease table, and the turn-workflow-side task-queue computation are all still unbuilt. `gateway/discord.md`'s own Notes Log has the Discord-specific side of this (the `/users/@me` resolution point, single-bot-token Helm shape still being what's actually deployed).
- 2026-08-24 (even later still): **Full cross-service path confirmed live**, closing the one gap the entry below left open — rebuilt/republished `loop-worker` and `tenant-worker` (not just `gateway`), then, rather than trusting the rebuild alone, ran a real `submitMessageEvent` call against the real cluster with a real `ParentSessionKey` set (the real production session as parent) and watched for the result: a real `CoordinatorWorkflow` started on the real deployed `loop-worker`, which really dispatched `SeedChildSessionContext` through Temporal's own task-queue mechanism to the real deployed `tenant-worker`, which really wrote a `context_summaries` row containing the real parent session's real content — all within about a second, verified by polling Postgres for the result rather than assuming success from the absence of an error. This is the strongest verification this feature has had: not a direct function call standing in for the real path, the actual real path.
- 2026-08-24 (yet later): **LCM-copy genesis context injection built** — closes the last open piece of "Resolved: Multi-Session Channels" (see "Context injection at genesis" above for the full detail). New: `activities/activities/seed_child_session.py` (`SeedChildSessionContextActivity`, registered in `tenant_worker.py`), `coordinator.go`'s `CoordinatorInput.ParentSessionKey` + the activity call gating on it, `inbound.go`'s `submitMessageEvent` now threading its own genesis check (`sessionsTag.RowsAffected() > 0`) into that field rather than just using it for the `parent_session_key` column write. Reuses `lcm.assemble()` verbatim rather than writing new context-reading logic — the exact function `ModelCall` itself calls, so there's no second, divergent way of reading a session's context. Live-verified two ways: the Go build (workflow/activity wiring, gating logic) against real compile/vet; the Python activity's actual behavior by calling it directly against the real cluster's real, already-running production session (16 real messages, real tool-call history) as the parent, confirming the child got a real leaf `context_summaries` row containing that actual content. **Not yet verified**: the full cross-language path through a real `CoordinatorWorkflow` execution (Go workflow calling the Python activity via a real Temporal task-queue dispatch) — that needs loop-worker and tenant-worker both running rebuilt images, not just a direct Python function call.
- 2026-08-24 (later): **Multi-session channels resolved** — `Discriminator`/`ParentSessionKey` added to `MessageEvent`, new "Resolved: Multi-Session Channels" section above. Driven entirely by planning the Discord platform (`gateway/discord.md`): its reply-threading requirement doesn't fit "one flat session per channel" at all, and working through it in direct conversation surfaced several real, generalizable decisions rather than Discord-specific ones — worth recording that the reasoning chain itself is what got them right, not any single insight in isolation: (1) rejected a genuinely random/generated session id specifically because it would need a `session_key → workflow` lookup table to resolve later replies, reintroducing exactly the `session_routing` table this doc's own history (2026-08-07 entry) already removed as unnecessary — a deterministic `<type>:<id>` discriminator format avoids that entirely; (2) rejected modeling a reply-session as a genuine Temporal child workflow of the channel's main session, once it became clear `CoordinatorWorkflow`'s own idle-timeout-and-recreate lifecycle can't function as a stable Temporal "parent" the way a subagent's genuinely-bounded child turn can — the graph lives in Postgres (`sessions.parent_session_key`) instead; (3) confirmed the mechanism is inherently arbitrary-depth (a plain parent-key chain), and that Discord's own flat two-level outcome is that platform's *policy choice* (collapse a reply chain to its root), not a limit of the mechanism — a platform wanting real nesting would resolve to the immediate parent instead, at the cost of a lookup Discord's approach doesn't need; (4) landed on copying the parent's current LCM state (not a raw recent-message window) as genesis context for a new child session, since it's a straight Postgres-to-Postgres duplicate (an activity, fits the reference-passing contract) that gives the child the parent's entire folded understanding rather than just recency — with an honest fallback (no LCM state exists yet if the parent's never had a real triggering turn) and an acknowledged, deliberately-deferred staleness gap (LCM snapshot is "as of the parent's last real turn," not "right now"). Design only — no code for any of this yet; `gateway/discord.md` (new) carries the rest of the Discord-specific design (ambient-message buffering, mention/reply-only response scope, auth, connection model).
- 2026-08-24: **Resolved the generic `MessageEvent` shape** — the last open item from this doc's original restructuring, closed once a second real question (a real user's "what is the linux version" message, sent through the actually-deployed chat page, completing a real `CoordinatorWorkflow` in 3.71s with a real `shell_exec` tool call) confirmed the Web implementation itself as genuinely working end to end, making it worth writing the shape down for real rather than leaving it speculative. The user's own framing caught a real gap in an initial draft: `channel_id` and `user` need to be separate fields, not one — a session is scoped to the channel/conversation, not to whichever user sent a given message, which only fails to matter for Web (no group-chat concept there). See "Resolved: Generic `MessageEvent` Shape" above for the field list and reasoning. Implemented as a refactor of `workflows/cmd/gateway`: extracted `inbound.go` (`MessageEvent`, `submitMessageEvent` — steps 3-6, now platform-agnostic) out of `send.go` (now just step 2, Web's own normalize), and generalized `auth.go`'s `sessionKeyFor` to take `(platform, channelID)` instead of just a Clerk user_id — the Web session_key *format* itself was deliberately left unchanged (still `agent:main:web:user:{id}`) to avoid orphaning the already-running real session from the same verification. Live-verified directly against the real cluster (port-forwarded Temporal + the `abishekk` tenant's real Postgres): confirmed the refactored `handleSend` produces identical behavior (accept, dedup, real `CoordinatorWorkflow` start under the same session_key format), then cleaned up the test workflow/rows.
- 2026-08-23 (yet later): First real Gateway code — the Web platform (`gateway/web.md`'s own Notes Log has the full detail). Confirms this doc's per-tenant deployment model directly: `deploy/helm/agent-harness-tenant`'s new `gateway.*` values block and `templates/gateway-{deployment,service,secret}.yaml` follow `tenantWorker`'s exact pattern (own Postgres connection, own credential Secret, `gateway.enabled: false` by default). The Postgres connection lease this section describes for connection-based platforms was NOT built in this pass — nothing needs it yet (Web is webhook-like, not connection-based) — left as a real open TODO for whichever connection-based platform gets built first, not silently dropped from the design.
- 2026-08-23 (later still): **Deployment topology revised — per-tenant, not shared-per-platform**, resolved through direct conversation prompted by finally answering the long-dangling "Gateway language/runtime" question from `gateway/web.md`. The user chose Go directly; checking that choice against `multi-tenancy.md`'s credential-isolation principle surfaced that this doc's *existing, already-resolved* "one gateway type per platform, shared across tenants" framing had never actually been reconciled against that principle at all — a real conflict, not a new one introduced by choosing Go (see "Superseded: One Gateway Type Per Platform, Shared Across Tenants" above). Resolved: one Gateway process per tenant, Go, every configured platform kind as goroutines in the same process — directly mirroring `loop-worker`'s own already-proven "one Client+Worker pair per tenant namespace, static config" pattern, just per-tenant instead of per-namespace-in-a-shared-pool. This in turn made the original Shard Assignment section's StatefulSet/shard-formula design largely moot (a per-tenant gateway essentially never approaches Discord's real sharding threshold) — replaced with a much smaller mechanism, a Postgres connection lease reusing `session_filesystem_leases`/`leases.py`'s exact renewal-based compare-and-swap pattern, which also unlocks something the shard design didn't have: an ordinary multi-replica `Deployment` for connection-based platforms too, not just webhook-based ones. Along the way, considered and correctly ruled out "just accept two simultaneous connections, dedup handles it" — true for inbound message correctness (`ingested_messages`' own PRIMARY KEY already provides that for free) but not for the separate, real problem of platform-side session flapping and ambiguous delivery-target selection, which is what the lease actually protects against. Design only at the time this entry was written — see the entry above (2026-08-23, yet later) for the first real implementation.
- 2026-08-23: Restructured into an index doc, at the user's direction, on the observation that each gateway kind interfaces with its client completely differently but shares the same downstream contract (SignalWithStart, deliver-as-Temporal-worker, the state-layer tables). Split platform-specific design out into `gateway/<kind>.md`, starting with `gateway/web.md` — the first real platform actually being built (a chat page in `agent-web`, currently a memory-explorer UI for agent-brain). Everything Discord-illustrated in this doc (Shard Assignment, Failure Analysis) stays here as general connection-based-category reasoning, not promoted to its own file, since no connection-based platform is a real build target yet — matches how `dreaming.md`/`multi-tenancy.md` were only ever promoted out of `future-work.md` once they became active, resolved work, not before.
- 2026-08-07: Full gateway design resolved from scratch (previously a bare scaffold): platform-type-based deployment model, always-async scope boundary (sync-required platforms explicitly unsupported), task-queue-based delivery replacing the originally-planned outbound broker, static-config shard assignment replacing a considered-and-rejected dynamic lease table, StatefulSet-vs-Deployment tradeoff made explicit per platform category, and a two-case failure analysis establishing that inbound loss is real (platform-resume-bounded) while a redundant gateway-side Postgres write does not help. Introduces `ingested_messages` (inbound dedup ledger) and `gateway_shard_state` (last-sequence-number, for resume) as new state-layer tables; removes `session_routing` as unnecessary. See `components/state-layer.md` and `components/activities-outbound-delivery.md` for the corresponding schema/activity changes.
