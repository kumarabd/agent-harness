# Component: Context Slot
## (Short-Term Memory — Session-Scoped Working Context)

> STATUS: IN PROGRESS — scope, compression strategy, retrieval triggers, system-prompt structure, summary storage schema, and session-start retrieval gating resolved (via LCM, Ehrlich & Blackman 2026) **and implemented**: `activities/activities/lcm/` (session-wide assembly, Three-Level Escalation compaction, and — new 2026-08-29 — the Memory-Access Tools `lcm_grep`/`lcm_describe`/`lcm_expand`), wired into `llm.py`'s `build_conversation`, `compress_context.py`, and `turn.go`. The compression trigger is no longer purely two-tier-threshold — soft fires unconditionally every iteration (2026-08-23 fix for a real lossiness bug; see Notes Log), hard remains threshold-gated. Verified end-to-end against a real cluster, not just unit-level — see Notes Log. Tool-output/large-content bounding is now **fully implemented** (2026-08-28): the storage/reference layer landed 2026-08-27 in `activities/activities/claim_check.py`, and the type-aware Exploration Summary on top landed today in `activities/activities/exploration_summary.py` — JSON (schema/shape extraction), CSV (columns + row count + sample), and unstructured text (deterministic line/word counts plus an optional LLM natural-language summary) all real, degrading gracefully to head/tail alone when no model is configured. The code-structural (tree-sitter) branch LCM §2.2 originally named remains deferred — deliberate, since real tree-sitter dependencies and reliable code-vs-text discrimination without a producer-supplied hint aren't warranted at current scope. Topic drift and threshold sizing remain open, deliberately deferred pending real usage data. **2026-08-29**: `lcm.py` split into a `lcm/` package (`constants.py`/`assembly.py`/`compaction.py`/`retrieval.py`), the Memory-Access Tools built (see "Resolved: Memory-Access Tools" below), and a real production bug fixed along the way — folding used to `DELETE` superseded summary rows, breaking losslessness for any session that had already folded; see Notes Log.

### Role (one line)
Owns what actually goes into a turn's `ModelCall` prompt — assembly, bounding, and compression of the live working context, scoped to the **whole session**, not one turn — replacing today's unbounded single-turn verbatim replay and no-op compression stub with a mechanism that can survive a genuinely long-lived, multi-topic session.

### Why this exists (recap)
Two concrete gaps found while auditing the implementation:
- `activities/activities/llm.py`'s `build_conversation` replays every message and tool result for the *current turn only* (`WHERE parent_id = turn_id`), verbatim, unbounded.
- `activities/activities/compress_context.py`'s `CompressContext` activity is a literal no-op, already wired into the turn loop's token-threshold gate (`components/temporal-workflow.md`, "Resolved: Compression / Context Management") but doing nothing when it fires.

### Resolved: Scope Is the Whole Session, Not One Turn
Originally scoped to a single turn (matching `build_conversation`'s current query). Revised after establishing two things:
- **The session idle timeout does not bound this.** `coordinator.go`'s `idleTTL` governs whether the *Coordinator Workflow execution* stays alive — when it fires, the workflow exits and a fresh `SignalWithStartWorkflow` recreates it later. Sessions themselves are **never deleted** (`components/session-filesystem.md`, `components/dreaming.md`), so a session used intermittently for months accumulates turns in Postgres indefinitely, completely independent of how many times its coordinator has idled out and respawned. Workflow-lifecycle and data-volume are unrelated axes.
- **Cross-turn continuity doesn't exist at all today, so this isn't scope creep — it's a real gap.** A new top-level `turn_id` is minted only when there's no currently-active turn (`coordinator.go`), and `build_conversation` is scoped strictly to one `turn_id`. So once a turn completes and the user sends a new message, that next turn starts with zero awareness of the just-finished exchange, even seconds later. Session-scoping this component is the fix.

**Where this draws the line against `components/memory-slot.md`:** context-slot = everything *within* this session, however long it actually lives; memory-slot = everything *across* sessions. This matches human working memory vs. long-term memory more cleanly than a turn/session split would — working memory spans a whole conversation regardless of how long it runs; long-term memory is what survives *between* conversations.

**Two concrete consequences for implementation (not yet made, doc-only pass):**
1. `build_conversation`'s query needs to change from `WHERE parent_id = turn_id` to pulling every top-level turn under the session (`JOIN turns WHERE parent_type = 'session' AND parent_id = session_key`, ordered by `turn_seq` then `seq`).
2. Token/budget accounting (`cumulativeTokens` in `turn.go`) currently lives in `TurnWorkflow`-local memory, which resets every new turn since each turn is a separate child-workflow execution. Session-level accounting can't be carried in workflow memory across that boundary — it has to be recomputed from Postgres at the start of each reasoning step, the same place `build_conversation` itself now reads from. This is consistent with how the rest of this project already treats Postgres as the source of truth, not a new pattern.

**Explicit non-goal:** session-scope means *across top-level turns*, not *into subagent internals*. Subagent context isolation is already a deliberate, resolved decision (`components/session-filesystem.md`) — a subagent's intermediate exploration stays private, folded back only via its explicit result. This component doesn't touch that.

### Resolved: Duties and Strategies

**1. Prompt assembly.** Hybrid sliding window + **hierarchical summary DAG** (not a single flat rolling-summary block, per LCM §2.1): the last *K* reasoning steps stay verbatim; older content is folded into leaf summaries (a span of messages), which themselves get folded into condensed summaries (a summary of summaries) as they age further, **recursively — condensed summaries fold into higher-level condensed summaries too, once implemented (2026-08-23, see Notes Log)** — multi-resolution, so old content degrades gracefully instead of a single block growing without bound. This last property (bounded growth) wasn't actually true for the DAG's condensed layer until the recursive-fold fix; a session that never resets would otherwise have accumulated an ever-growing flat list of condensed rows instead of a properly logarithmic-height DAG.

**2. Tool-output / large-content bounding.** Two tiers, not one blanket rule — **both implemented** (`activities/activities/claim_check.py` + `activities/activities/exploration_summary.py`, wired into `shell_exec`; per-stream independent decision — big stdout with tiny stderr doesn't drag the small stream through the PV):
- Small tool outputs: inlined as `{"inline": <text>}`, no PV round-trip, no summary — the model just reads them directly. Threshold currently `claim_check.SMALL_OUTPUT_BYTES` (4096), placeholder.
- Large content: never loaded into context directly. Written to the PV under the tool's own session directory (`.claim-check/{tool_call_id}.{stream}.log`, isolation matches `components/session-filesystem.md`'s subagent-subtree model exactly) and returned as a reference: `{claim_check_path, size_bytes, head, tail, exploration_summary, note}`. The **type-aware Exploration Summary** picks its shape by content type: JSON → schema/shape (keys, value types, array lengths — pure parse, no LLM); CSV → columns + row count + sample rows (via `csv.Sniffer` for dialect detection, also pure parse); unstructured text → deterministic line/word/byte counts plus, when a model is configured, a short LLM natural-language summary that names the content type and any obvious structure/errors. The model reads whichever specific part of the full output it needs beyond the summary via ordinary `shell_exec` (`cat`, `head`, `tail`, `grep`) against `claim_check_path`. **Cost gating:** only the unstructured-text branch pays for an LLM call — structured content is described deterministically because a schema is a fact, not a guess. **Deferred, not built:** the code-structural (tree-sitter AST walk) branch LCM §2.2 originally named — a real tree-sitter dependency and reliable code-vs-text discrimination without producer-supplied hints aren't warranted at current scope; the text-tier LLM summary handles code adequately if less efficiently in the meantime.

**3. Compression trigger + execution.** Two tiers, not one constant (LCM §2.1, §2.3) — **revised 2026-08-23, see Notes Log**: the two tiers are no longer both gated on the same token threshold.
- **Soft tier**: fires unconditionally, every iteration, async — doesn't block the turn. No longer threshold-gated: `context_tokens` is structurally capped by the verbatim window (duty #1's *K* messages) and so can never reliably reflect how much real content has aged out of it — a threshold check against a capped quantity can't detect unbounded growth on the other side of the cap. Relies on `lcm.compact`'s own cheap no-op (nothing to do → one Postgres read, no LLM call) the same way `WriteMemory` already fires unconditionally every turn.
- **Hard threshold**: still genuinely threshold-gated and blocking — a separate, correctly-scoped concern (the model's actual context window is at risk *right now*), proportional to `ModelCallOutput.ContextWindow`.
- **Execution — real body for `CompressContext`**, replacing the no-op stub, via **Three-Level Escalation** for guaranteed convergence: Level 1 (LLM summarize, preserve details) → if it fails to shrink the content, Level 2 (LLM summarize, aggressive bullet points) → if that still fails, Level 3 (deterministic truncate, no LLM, always shrinks). This solves a real, previously-unnamed failure mode: a summarization call producing output *longer* than its input ("compaction failure").

**4. Compression-threshold sizing — resolved and implemented (2026-08-23).** `components/model-registry.md` now exists; `turn.go`'s `compressionState` computes soft/hard as a fraction (0.6/0.8, placeholder-simple) of `ModelCallOutput.ContextWindow`, the active model's real context window, falling back to the old static `softCompressionThreshold`/`hardCompressionThreshold` constants only on the fixture path (`ContextWindow == 0`, since fixtures never resolve a real model).

**5. Cache-prefix stability.** Compaction only ever rewrites the aging tail — the summary/DAG block and whatever's leaving the verbatim window — never reshuffles already-cache-stable earlier messages. The swap happens atomically between turns, so a compaction event costs at most one KV-cache regeneration, not per-message churn (LCM §2.4's footnote reasoning).

**6. Losslessness.** Nothing new needs to be built for this — it's already structurally true. Postgres's `messages`/`tool_calls` tables are already never deleted; they already *are* LCM's "Immutable Store." The Active Context (the assembled prompt) is a derived **view** over that data, not a second copy — compression only changes what's shown to the model, never what's stored.

**7. Scope boundary.** Session-scoped (§ above), explicitly excluding subagent-internal content.

### Resolved: Retrieval Is Event-Triggered, Not Continuous
Two triggers cause memory-slot content to enter context-slot's assembled prompt — no third, automatic, always-on relevance pass:
- **Session start** — automatic, no model decision. Gated on "zero prior turns exist for this `session_key`," not on Coordinator respawns (which are workflow-lifecycle noise, unrelated to whether this is genuinely a new line of inquiry).
- **Mid-session, model-initiated** — the model calls a tool (`components/tool-registry.md`'s concern — `search`/`expand`), reading `components/memory-slot.md`'s direct agent-brain integration, folded back as an ordinary tool observation. No special-casing versus any other tool call.

**Explicitly rejected: re-evaluating relevance on every `ModelCall`.** This would mean the set of included blocks could change every reasoning step, which directly breaks cache-prefix stability (duty #5) — every step becomes a fresh cache miss. Continuous re-judgment and cache stability are in direct tension; cache stability wins.

**Explicitly deferred, not designed this pass:** a cheap, deterministic (non-LLM), always-on relevance *pre-filter* — analogous to preattentive/"cocktail party effect" salience detection in human attention: continuous, but cheap and parallel, categorically different from expensive deliberate reasoning. It would decide *whether* to trigger retrieval, never touch the prompt prefix itself, and so wouldn't reintroduce the cache-busting problem the rejection above avoids. Parked as future work — not required for this design to function, and consistent with this project's standing discipline of not building ahead of real usage data.

**Removal is a separate concern from addition, and stays that way.** Relevance triggers what gets *added*. What gets *smaller over time* is handled entirely by duty #3's token-budget-driven compression, uniformly, regardless of whether content originated from the session's own turns or from a memory-slot retrieval. There is no second "is this still relevant" pass that actively evicts content — everything ages via the same mechanism.

### Resolved: System Prompt Is Tiered, Not Monolithic
Two tiers, not one flat string:
- **Core identity** — small, static, always present, every `ModelCall`, unconditionally. This is today's `DEFAULT_SYSTEM_PROMPT`/`sessions.system_prompt` mechanism (`activities/activities/llm.py`), unchanged in shape — just deliberately kept small. No retrieval, no selection logic; the cost of getting this wrong (dropping a core constraint mid-conversation) is too high to make it conditional, and it's cheap enough that there's no compression benefit to stripping it.
- **Situational content** — persona/behavior preferences and domain-specific framing are *not* crammed into the static system prompt. They're typed, selectively-retrieved content sourced from `components/memory-slot.md` (`type: persona-rule`, `type: domain-fact`, alongside `type: episode`), entering context via the same two triggers above, not a separate mechanism. Placed and labeled distinctly from live session turns (not silently merged) so the model's own recency/authority weighting can naturally resolve any tension between old retrieved content and what the session has since established.

This reframes "domain shift" (e.g. a health-related question pulling in health-appropriate framing) as an ordinary memory-slot retrieval, not a dynamic system-prompt rewrite — and reframes "not every behavior rule applies to every task" as the same relevance-filtering problem context-slot already solves for conversation history, just pointed at a second input.

### Resolved: LCM as the Concrete Mechanism
Adopted directly, not just as inspiration, with two adaptations for how this project actually runs:

| LCM concept | Maps to | Note |
|---|---|---|
| Immutable Store | Postgres `messages` / `tool_calls` | Already never deleted — nothing new to build |
| Active Context | Context-Slot's assembled prompt | Session-scoped here, not turn-scoped |
| Summary DAG (leaf / condensed) | Context-Slot's compression output | New `context_summaries` table — see "Resolved: Summary Storage Schema" below |
| Soft / hard threshold | Extends `compressionGateTokens` into two tiers | One constant today → soft (async) + hard (blocking) |
| Three-Level Escalation | Real body for `CompressContext` | Replaces today's no-op stub; guarantees convergence |
| `lcm_grep` / `lcm_describe` / `lcm_expand` | `activities/activities/lcm/retrieval.py` — the Memory-Access Tools, `components/tool-registry.md`'s registry entries over Postgres + the DAG | **Built 2026-08-29 — revised back to matching LCM's own original guardrail**, not the memory-slot.md parallel this row previously (2026-08-22) claimed: `lcm_expand` IS subagent-only, enforced at the tool-schema level (excluded from the list entirely for a main-agent turn, not merely discouraged) — see "Resolved: Memory-Access Tools" below for why this diverged from `memory-slot.md`'s `search`/`expand` after all |
| The "engine" (one continuous process) | Recomputed per `ModelCall` activity, fresh from Postgres | Each turn is a separate Temporal child-workflow execution — no long-lived process to hold state in |

LCM's own paper states its storage layer only needs transactional writes, referential integrity, and indexed full-text search — *"any storage backend satisfying these properties would suffice."* The per-tenant Postgres this project already runs satisfies that natively; adopting LCM's architecture isn't a new infrastructure dependency, just new logic over data already being persisted.

### Resolved: Summary Storage Schema
A dedicated table, not reuse of `messages`. A summary is a genuinely different kind of object from a conversational message, and this project has consistently given different kinds of objects their own tables rather than overloading an existing one with sparse, conditional columns (`session_filesystem_leases`, `_test_scripted_responses`).

```sql
CREATE TABLE context_summaries (
  summary_id    uuid PRIMARY KEY,
  session_key   text NOT NULL,
  kind          text CHECK (kind IN ('leaf','condensed')),
  covers        uuid[],   -- leaf: message_ids it summarizes; condensed: child summary_ids
  content       text,     -- the actual summary text
  token_count   int,
  created_at    timestamptz NOT NULL DEFAULT now(),
  folded_into   uuid REFERENCES context_summaries(summary_id)  -- added 2026-08-29, see Notes Log
);
```

`folded_into` (2026-08-29): NULL means "still part of the current DAG frontier" — what `assemble()` shows, what a later fold pass considers re-folding. Set once a row has been folded into a newer condensed row. Added specifically so folding never deletes a row — see "Resolved: Memory-Access Tools" below and the matching Notes Log entry for the real bug this replaced.

### Resolved: Memory-Access Tools
LCM's own paper (Appendix C.1) names three tools over the DAG: `grep`, `describe`, `expand`. Built 2026-08-29 in a dedicated `activities/activities/lcm/retrieval.py`, alongside the rest of the `lcm/` package (`constants.py`/`assembly.py`/`compaction.py`/`retrieval.py` — a clean split of the former flat `lcm.py`, one file per concern, `__init__.py` re-exporting the exact same flat surface every existing caller already used so the split is invisible outside the package).

- **`lcm_grep(pattern, mode, limit)`** — session-scoped. `mode="pattern"` (default) is real Postgres regex (`~`); `mode="fulltext"` is the `messages.search_vector` GIN-indexed tsvector column (live since `001_initial_schema.sql`, unused by any code until this) via `plainto_tsquery`/`ts_rank` — stemmed English keyword search, not semantic/embedding search (no vector column exists on `messages`; out of scope). `limit` defaults to `GREP_DEFAULT_LIMIT` (20) but has **no system-enforced hard ceiling** — a deliberate choice (the model can ask for as many results as it has real reason to want; the default exists only to stop an unscoped query from flooding context by default). Each result reports `covered_by_summary_id`: the summary node **currently** covering that message on the live DAG frontier (following `folded_into` upward if the message's original leaf has itself since been folded into a condensed summary), or `null` if the message is still directly visible in context as-is.
- **`lcm_describe(id)`** — auto-detects whether `id` is a `message_id` or a `summary_id` (the two tables share no id namespace) and returns the matching shape. Acts only on the DAG/raw messages — never touches `claim_check.py`'s large-tool-output files, which already have their own recovery route via `shell_exec`.
- **`lcm_expand(summary_id)`** — recursively resolves a summary (leaf or condensed) down through the DAG to the real, original messages it represents, returned as plain `{role, content}` pairs. **Subagent-only, enforced at the tool-schema level** (`llm.tools_schema_for(is_subagent)` excludes it from `TOOLS_SCHEMA` entirely for a main-agent turn, rather than listing it and rejecting the call at runtime) — letting the main agent freely re-expand every summary back to full text would fight directly against the reason compaction exists. This is a genuine divergence from `components/memory-slot.md`'s `search`/`expand` (both unrestricted there) — that doc's `memory_expand` has no depth parameter to escalate through, so the unbounded-re-expansion risk motivating this restriction doesn't apply to it the same way; `lcm_expand` walking an arbitrarily deep condensed-of-condensed chain is a different shape of risk. `lcm_grep`/`lcm_describe` carry no such restriction.

Correctness of `lcm_expand`'s recursive walk depends entirely on the `folded_into` fix above — see Notes Log for the real bug this replaced and how it was verified.

### Resolved: Session-Start Retrieval Is Unconditional
Every zero-prior-turn session triggers the memory-slot retrieval call, no cheap pre-check gate. Simpler, and never misses a case where retrieved memory would have helped, at the accepted cost of a wasted call on trivial first messages ("hi," "what's 2+2"). Revisit only if real usage data shows this cost matters in practice — consistent with this project's standing discipline of not building a gating mechanism ahead of evidence it's needed.

### Open Questions / To Design
- **Topic drift within one very long-lived session** — whether trusting aging-based deprioritization (older content sinks further into the DAG) is sufficient, or whether something more explicit is eventually needed. Deliberately left untouched — session_key is a channel identity, not a topic marker (`sessions.session_key` example: `agent:main:discord:guild:123`), so this is a real risk, just not one with evidence yet of needing a dedicated mechanism.
- **The deferred cheap Tier-1 salience pre-filter** (see "Retrieval Is Event-Triggered" above) — explicitly parked, not designed this pass.
- **Soft/hard threshold numeric values** — not sized yet, same treatment as every other numeric threshold in this project (heartbeat intervals, HPA, `max_iterations`): a placeholder now, tuned once real usage data exists.
- ~~**Sequencing with sibling components** — this doc references `components/memory-slot.md`'s typed retrieval and `components/tool-registry.md`'s `search_memory`/`lcm_expand`-equivalent tools, neither of which has landed its own resolved design yet.~~ Both landed: `memory-slot.md`'s `memory_search`/`memory_expand` and this doc's own `lcm_grep`/`lcm_describe`/`lcm_expand` (2026-08-29, see "Resolved: Memory-Access Tools").

### Notes Log
- 2026-08-28: Implemented duty #2's type-aware Exploration Summary layer on top of yesterday's claim-check storage — new `activities/activities/exploration_summary.py`, wired into `claim_check.store_if_large` (now async). Detection order is cheap → expensive: binary sniff (NUL byte in first 8KB), then `json.loads`, then `csv.Sniffer` with disqualifying checks (≥2 rows, ≥2 columns — bare `Sniffer.sniff` on prose otherwise gives false positives), then text fallback. JSON/CSV are pure parses (zero model calls, deterministic); only the text branch calls the model, and only when an `openai_client` + `model` are actually available — degrades to line/word/byte counts alone otherwise, same graceful-degradation pattern `agent_brain.py`/`mcp_hub.py` already use for their own unconfigured cases. Threaded `openai_client` + a resolved default-tier model through `ToolCallActivity → ToolContext → claim_check.store_if_large → exploration_summary.summarize` (fixture-only local runs pass no client and still work). Bounded outputs throughout (per-type caps on keys/columns/samples, an LLM input cap so we never ask the model to summarize a whole 100MB blob, and a cap on the LLM's own returned summary length) so the summary itself can't become the thing pushing context over the limit. Code-structural (tree-sitter) branch explicitly deferred — real dependency + reliable code-vs-text discrimination without a producer hint aren't earning their keep at current scope.
- 2026-08-23 (even later, prompted by planning the Gateway's Web platform): While designing a Discord-like "one continuous session, never explicitly reset" UX for the new Web platform (`components/gateway.md`), a real cost concern was raised: wouldn't a session that never resets accumulate ever-more-frequent compactions and ever-growing token consumption compared to periodically starting fresh? Checked directly against `lcm.py` rather than assuming either way: per-turn cost *is* already bounded by the verbatim window + summary DAG (never full raw history) — but `_fold_leaves_if_due` only ever folded `kind='leaf'` rows; condensed rows were never re-folded once `LEAF_FOLD_THRESHOLD` (5) of them existed. `assemble()` sums every summary row's `token_count` unconditionally into every call's context, so a long enough never-reset session really would have accumulated an unboundedly growing flat list of condensed rows — the exact "single block growing without bound" duty #1 already claimed the hierarchical design avoided, just one level higher than leaves. **Fixed**: generalized the fold into a recursive `_fold_leaves_if_due` → `_fold_kind_if_due('leaf')` then a loop over `_fold_kind_if_due('condensed')` while condensed count is still at/above threshold — no schema change needed, `context_summaries.covers` was already documented as supporting "condensed: child summary_ids." Verified live against real Postgres and a real summarization call (not just code-reviewed, unlike the equivalent caveat in the entry below this one): hand-seeded 5 fake leaf rows, confirmed they folded into 1 condensed row; seeded 4 more condensed rows to reach 5, confirmed a second real call correctly folded all 5 into one higher-level condensed row with real summarized content, and confirmed the recursive loop correctly stopped once only 1 condensed row remained. This closes the actual root cause behind the cost concern, so the Gateway's Web platform can keep the always-on, never-loses-context design without an unbounded long-run cost tradeoff — see `components/gateway.md`'s own notes for that decision.
- 2026-08-23 (later still): **Found and fixed a real, previously-undiscovered lossiness bug** in duty #3's compression trigger, while building and running a full-stack "deep conversation" validation suite (`workflows/scenarios/deep-conversation/`) that deliberately exercised many real turns in one session. Both soft and hard tiers were gated on the same `context_tokens` — but that value is `assemble()`'s *windowed* size, hard-capped at `VERBATIM_WINDOW_MESSAGES` (20) regardless of total session length. A session with many short turns can keep `context_tokens` under the soft threshold indefinitely while real content silently ages out of the 20-message window with **zero summary ever written to preserve it** — the opposite of what "Lossless" Context Management is supposed to guarantee, and a real gap the token-threshold trigger has no way to detect (a threshold check against a structurally-capped quantity can't see growth happening beyond the cap). Confirmed directly, not just reasoned about: ran 17 real turns in one session (`deep-conv-1`, in a real Docker container against the real cluster — see below), stated a fact once (turn 4), watched it fall genuinely outside the verbatim window by turn 17 (window covered only turn_seq 8–17, confirmed via direct query) with `context_summaries` still empty, and confirmed the only reason the model still answered correctly was a coincidental restatement at an intervening turn keeping it artificially in-window — without that restatement the fact would have been unrecoverable.

  Considered and rejected a fix that added a new `has_uncovered_overflow` signal threaded through `lcm.assemble` → `llm.build_conversation` → `ModelCallOutput` → Go, gated into the dispatch alongside the existing tiers — it worked but added a second trigger dimension plus a new Postgres query on every `ModelCall` just to compute a boolean. **Landed on a simpler fix instead**: `lcm.compact` already self-gates correctly (logs "nothing new to compact" and no-ops for free when there's truly nothing to do — verified working since the entry below this one). So `turn.go`'s soft tier no longer checks a threshold at all — it fires unconditionally, every iteration, and lets `compact`'s own no-op absorb the cost of "nothing to do" turns, the same "simplicity over a wasted-call-cost pre-check" call this doc's own "Resolved: Retrieval Is Event-Triggered" section already made for session-start memory retrieval. Zero Python changes needed; the entire fix is a ~15-line change to `turn.go`'s existing dispatch switch. Hard tier is untouched — it's a genuinely separate, correctly-scoped concern (protect the model's real context window right now).

  Verified live in the same session that found the bug: rebuilt the loop-worker image, ran one more real turn, confirmed via `tenant-worker` logs that `lcm.compact` fired and wrote a real leaf summary (`"wrote leaf summary covering 20 messages"`), confirmed via direct Postgres query that the summary's `covers` array genuinely included the turn-4 fact-statement message (so the fix caught it before it could be dropped), and confirmed the model still answered the recall question correctly afterward, now via the summary block rather than a lucky restatement. Note: the *summarized* content didn't literally restate the fact verbatim — an inherent, already-named property of Level 1 LLM summarization ("preserve important details" is a judgment call, not literal fact-by-fact retention), separate from and not fixed by this change; the fix guarantees the content reaches compaction before being dropped, not that compaction is perfectly lossless. See `workflows/scenarios/deep-conversation/README.md` for the full suite this was found by.
- 2026-08-29: **Built the Memory-Access Tools** (`lcm_grep`/`lcm_describe`/`lcm_expand`) and, along the way, **found and fixed a real, already-in-production losslessness bug**: `_fold_kind_if_due` (now `lcm/compaction.py`) used to `DELETE FROM context_summaries` for every row it folded, right after inserting a new condensed row whose own `covers` array pointed at those same now-deleted `summary_id`s. Any session that had already crossed `LEAF_FOLD_THRESHOLD` (5 leaf summaries) had *already* lost the ability to recover the original content behind that fold — not a hypothetical, the DELETE had already run for real sessions before this fix shipped; this fix only stops it recurring going forward, it can't retroactively undelete what a prior fold already removed. Fixed via a new nullable `context_summaries.folded_into` column (migration `010_context_summaries_folded_into.sql`, dual-copied to `deploy/helm/agent-harness-tenant/files/` per the standing convention) — folding now `UPDATE`s `folded_into` instead of deleting, inside the same transaction as the new condensed row's insert. `assemble()` and `_fold_kind_if_due`'s own re-fold query both gained an `AND folded_into IS NULL` filter so a folded row stops counting toward "the current DAG frontier" while staying physically present for `lcm_expand` to walk through.

  Also refactored the former flat `activities/activities/lcm.py` into a `lcm/` package (`constants.py`/`assembly.py`/`compaction.py`/`retrieval.py` + `__init__.py` re-exporting the identical flat surface — `compress_context.py`/`llm.py`/`seed_child_session.py` needed zero changes). Wired the three new tools into `tools.py`'s `TOOL_REGISTRY` (Tier B-shaped but tighter timing than the network-tier tools next to them — 15s `start_to_close` vs. `memory_search`/`search_tools`'s 30s, reflecting a local Postgres read vs. a network call) and into `llm.py`'s `TOOLS_SCHEMA`, plus new `llm.tools_schema_for(is_subagent)` (replacing the two flat `llm.TOOLS_SCHEMA` references in `model_call.py`) so `lcm_expand` is excluded from the schema entirely for main-agent turns — `model_call.py` now does one extra indexed lookup on `turns.parent_type` per `ModelCall` to know which schema to hand the provider.

  Verified live against the real `abishekk` tenant Postgres (not just code-reviewed): applied migration 010 for real via `kubectl exec` + `psql` (matching the Helm migrate-hook's own apply-then-record-in-`schema_migrations` sequence, so a future `helm upgrade` sees it already applied and skips it cleanly); ran a throwaway, fully-rolled-back transaction seeding two real messages, a leaf summary covering them, and a condensed summary folding that leaf — confirmed `assemble()`'s query now shows only the condensed row (leaf correctly excluded), confirmed the leaf row's own `covers` (the real message_ids) is still reachable through the condensed row's `covers` (proving the fix — under the old DELETE behavior this exact query would have returned zero rows), confirmed `lcm_grep`'s pattern-mode regex and fulltext `tsvector` queries both match real content, and confirmed the folded_into chain walk resolves a message's "currently active" covering summary to the condensed id, not the stale folded leaf id. Also verified at the Python/runtime level (not just SQL): real imports of the new `lcm/` package surface, `TOOL_REGISTRY` entries, and `llm.tools_schema_for` correctly excluding `lcm_expand` from a main-agent schema while including it for a subagent one — all via the real venv, not just `py_compile`. Not yet verified against a live model actually calling these tools end-to-end through a real turn (would need a redeploy, not done this pass — same category of gap this project's own docs consistently flag rather than silently skip).
- 2026-08-23 (later): Closed the one gap the prior entry left open — `CompressContext`'s real dispatch path (soft/hard trigger → `CompressContextWorkflow` → the activity → `lcm.compact`) had only been exercised by calling `lcm.compact` directly, bypassing Temporal. Verified for real this time: built a real session with a small (but genuinely real, tenant-configurable) `LANGUAGE_MEDIUM_CONTEXT_WINDOW=8000` and enough real turns to organically cross soft/hard, watched `CompressContext` actually fire via the real trigger 4 times, correctly no-op three of them (message count still ≤ `VERBATIM_WINDOW_MESSAGES`, so nothing existed outside the verbatim window yet — a real, previously-unconsidered interaction between the token-threshold trigger and the message-count window), then correctly write a real leaf summary once message count exceeded the window. `assemble()` confirmed picking the summary back up afterward. Level 2/3 escalation and leaf-folding remain unexercised (Level 1 kept succeeding; only 1 leaf summary was produced).

  **Found along the way, unrelated to this component**: `coordinator.go`'s own documented stub (`turnSeq := 0 // ... stubbed to 0 here`) causes every fresh `CoordinatorWorkflow` execution (i.e. any time the prior one has idled out, `idleTTL`=30s) to start a brand-new top-level turn numbered `turn:1` again, regardless of how many real turns the session already has — a real turn_id collision for any session with more than ~30s between messages, not a test artifact. Confirmed directly: 13 real, separately-sent messages all landed under the single reused `turn_id="...:turn:1"` row instead of `turn:1..13`. Not fixed here — flagged for `components/temporal-workflow.md`/`components/session-coordinator.md` to pick up. **Fixed 2026-08-23** — see `components/session-coordinator.md` Notes Log for the fix and live verification.
- 2026-08-23: **Implemented** the core LCM mechanism, not just designed it — `activities/activities/lcm.py` (session-wide `assemble`, two-tier `compression_state`, Three-Level Escalation `compact`), replacing `llm.py`'s old turn-scoped `build_conversation` query and `compress_context.py`'s no-op stub with real bodies that call into it. Added `002_context_summaries.sql` (new migration; extended `postgres-migrate-hook.yaml` to apply migrations in filename order via a real `schema_migrations` tracking table, closing a separate open gap — the migrate hook previously only knew how to apply exactly one hardcoded file). `turn.go`'s single `compressionGateTokens` constant became `softCompressionThreshold`/`hardCompressionThreshold`; the compression check moved from a pre-ModelCall gate on the turn-local `cumulativeTokens` accumulator to a post-ModelCall check on a new `ModelCallOutput.ContextTokens` field (computed fresh in Python each call, since `cumulativeTokens` can't survive across separate turn-workflow executions the way the doc's own "Two concrete consequences" section already flagged — `budgetTokens`, a different concern, turn-scoped API spend rather than context size, was left untouched). Soft-tier compaction dispatches via a new `CompressContextWorkflow` detached child workflow (`ParentClosePolicy: ABANDON`), mirroring `WriteMemoryWorkflow`'s already-fixed pattern for genuine fire-and-forget — a bare unawaited activity isn't reliable when the calling turn workflow closes moments later. Token counting is a plain `len(text)//4` heuristic, not a real tokenizer (no single correct tokenizer to target across providers).

  Verified end-to-end against the real cluster, not just at the unit level: a two-turn session (`test-lcm-1`), each turn a genuinely separate top-level turn/child-workflow execution, where turn 2 ("what's my favorite color?") correctly answered from turn 1's content ("teal") purely via session-wide assembly, confirming the "Resolved: Scope" fix actually closes the gap it was meant to. Also directly exercised `lcm.compact` against that same real session — a real LLM summarization call succeeded on the first (Level 1) attempt, produced a correct, genuinely compressed summary, wrote it to `context_summaries`, and a fresh `assemble()` call correctly picked it up. Level 2/Level 3 escalation and the leaf→condensed folding threshold were not exercised (would need either a summarization failure or 5 real compactions to trigger naturally) — code-reviewed but not live-tested.

  Not built this pass, staying explicitly out of scope: tool-output/large-content bounding's type-aware Exploration Summary + claim-check reference (duty #2) — only the pre-existing flat truncation for `shell_exec` output exists; compression-threshold sizing as a function of the active model's real context window (duty #4, blocked on `components/model-registry.md`, which doesn't exist in code); the deferred cheap non-LLM relevance pre-filter (unchanged, still parked).
- 2026-08-22: Fixed a real inconsistency surfaced while revising `memory-slot.md` to drop its generic backend-interface framing (that doc is now explicitly opinionated to agent-brain, not a pluggable abstraction): the LCM mapping table here still claimed `lcm_expand`'s subagent-only restriction applied, which `memory-slot.md` had already superseded (both `search`/`expand` unrestricted). Corrected to match. This doc's own LCM adoption was already stated as direct, not hedged as swappable, so no equivalent pivot was needed here — just this one stale cross-reference.
- 2026-08-17 (later): Resolved summary storage as a dedicated `context_summaries` table (leaf/condensed rows, `covers` array pointing at messages or child summaries), not a reuse of `messages`. Resolved session-start retrieval as unconditional — no cheap pre-check gate, simplicity and never-miss preferred over the wasted-call cost on trivial first messages, revisit only with real usage evidence.
- 2026-08-16: Introduced as a scaffold, one of five components (context-slot, memory-slot, model-registry, tool-registry, budget-guardrails) identified while discussing how to make the harness robust for genuinely complex, multi-step tasks. Grounded in `llm.py`'s unbounded replay and `compress_context.py`'s no-op stub — not yet designed.
- 2026-08-17: Resolved scope (whole session, not one turn — cross-turn continuity turned out not to exist at all today, so this closes a real gap, not scope creep), the seven core duties (assembly, tool-output bounding, compression trigger/execution, threshold sizing, cache stability, losslessness, scope boundary), retrieval as event-triggered rather than continuous (rejected on cache-stability grounds; a cheap non-LLM pre-filter explicitly deferred, not designed), a tiered system prompt (small static core identity vs. typed, selectively-retrieved situational content), and adopted LCM (Ehrlich & Blackman, Voltropy, Feb 2026 — "LCM: Lossless Context Management," benchmarked against Claude Code on OOLONG) as the concrete mechanism for nearly all of the above, evaluated against and preferred over three other reference points considered first (Karpathy's "LLM Wiki" gist, OpenKB, PageIndex — all found to be durable/cross-session knowledge-base tooling, better fits for `components/memory-slot.md` than this component). Storage schema for the summary DAG remains the single biggest open gap.
